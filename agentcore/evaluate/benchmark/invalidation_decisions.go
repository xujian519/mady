// Code generated by scripts/extract_invalidation_cases.py; DO NOT EDIT.
// Source: 宝宸知识库_Raw 无效复审决定数据集（31562 件 CNIPA 无效宣告请求审查决定书）。
// Selected 100 representative cases covering invention/utility model/design patents
// and all invalid/partially invalid/maintained conclusions.

package benchmark

import (
	_ "embed"
	"encoding/json"
	"fmt"

	"github.com/xujian519/mady/agentcore/evaluate"
)

//go:embed invalidation_decisions.json
var invalidationDecisionCasesJSON []byte

// InvalidationDecisionCases 是第二层 Golden Benchmark：真实专利复审无效决定书。
// 从宝宸知识库_Raw 数据集的 31562 件无效宣告请求审查决定书中筛选出 100 件典型案例，
// 覆盖发明/实用新型/外观设计以及全部无效/部分无效/维持有效三种结论。
// 数据加载自同目录下的 invalidation_decisions.json（go:embed）。
var InvalidationDecisionCases []evaluate.TestCase

func init() {
	if err := json.Unmarshal(invalidationDecisionCasesJSON, &InvalidationDecisionCases); err != nil {
		// invalidation_decisions.json is embedded at compile time, so a parse
		// failure means a corrupted build or merge conflict residue. Fail loud
		// so CI catches it instead of silently making the benchmark incomplete.
		panic(fmt.Sprintf("evaluate/benchmark: failed to load invalidation decision cases: %v", err))
	}
}
