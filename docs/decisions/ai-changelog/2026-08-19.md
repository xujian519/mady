# 2026-08-19

## docs(specs): 技术债务修复实施计划（安全测试/门禁统一/复杂度/死代码）

新增 docs/specs/tech-debt-fix/04-tasks.md：将 2026-08-19 全量静态分析发现的技术债务拆解为 5 阶段 16 任务。阶段 1 安全敏感路径测试补齐（handoff executeDelegate 12%、citation_gate Validate 0%、levels 4 个 With 选项 0%）；阶段 2 子模块 lint 门禁统一（tui 59 issues、tools 108 issues 的假绿修复）；阶段 3 高复杂度函数拆分（runInnerLoop 92 / vectorSearchSQLParallel 91 / runCompaction 84 / graph.Run 78）；阶段 4 死代码清理（tool_domains.go 弃用别名、仅为测试保留的命令处理器、仅测试引用的工具函数）；阶段 5 重复代码消除。每任务含文件范围/验收/风险/审查要求，遵循 AGENTS.md 3-5 文件粒度与敏感路径 L3 审阅。同步更新 docs/specs/README.md 索引。

---

## test(guardrails): 阶段1：安全敏感路径测试补齐（handoff/citation_gate/levels/audit/sqlite/constitutional）

按 docs/specs/tech-debt-fix/04-tasks.md 阶段 1 执行：T1.1 handoff executeDelegate 12%→96%（结构化结果/纯文本回退/失败兜底/自定义文案 4 用例）；T1.2 citation_gate Validate 0%→100%；T1.3 levels 四个 With 选项 0%→100%（含 DeferredQueue 集成用例）；T1.4 domains/audit 新增 7 测试覆盖 46.9%、event_store RunID/Version/Migrate 0%→100%、constitutional_rules 16 函数全部 100%。附带修复 event_store.go CreatedAt 解析 bug（SQLite datetime('now') 无时区格式导致 time.Parse RFC3339 失败恒为零，改双格式解析）。验证：go build/vet/lint 0 issues，go test -race ./... 97 包全绿，tools/tui/desktop 子模块 race 全绿。

---

## chore(tools): 阶段2：子模块 lint 门禁统一（tui 59/tools 108 issues → 0，双路径一致 + CI 漂移检测）

按 docs/specs/tech-debt-fix/04-tasks.md 阶段 2 执行：T2.1 tui 门禁对齐（noctx CommandContext 修复、G602 索引防护、7 个 unparam 参数移除、renderOrderedList/colorParams 抽取、30 个 gocognit 带理由豁免，配置启用 gocognit/dupl/unparam/gosec/noctx）；T2.2 tools 门禁对齐（新增 desktop/browserproviders 包注释、closeSessionRequest 公共 helper、3 个函数签名简化、19 个 gocognit 豁免、根配置补充 12 条 errcheck defer 清理豁免规则）；T2.3 CI 新增 submodules-with-root-config 步骤防配置漂移。验证：四模块根配置与内部跑 lint 均 0 issues，go test -race 根模块 97 包 + tools 4 包 + tui 9 包全绿。

---

## refactor(agentcore): 阶段3：高复杂度函数拆分（runInnerLoop/vectorSearch/runCompaction/graph.Run/TUI 五热点）

按 docs/specs/tech-debt-fix/04-tasks.md 阶段 3 执行：T3.1 runInnerLoop 92→≤30（拆 runModelTurnSafe/recordModelResponse/finishWithoutToolCalls/runToolCallTurn/detectRepetition/runTruncationGuard 六方法）；T3.2 vectorSearchSQLParallel 91→≤30（拆 prepareVectorSearch/scanVectorRange/insertVectorCandidate/mergeVectorResults）；T3.3 runCompaction 84→5（拆 12 个辅助函数，消除 unparam 遗留）；T3.4 graph.Run 78→≤30（拆 runLayerNodes/nodeInputFor/applyNodeOutputs）；T3.5 TUI 五热点拆分（processMsg→3、renderVertical→4、On→3、renderBlock→15、renderFrame→20，On 改 map 驱动）。行为回归修复 2 处：PanicMsg 继续流向常规分发、delayedWindowSizeMsg unwrap 传回分发。验证：四模块 lint 0 issues、根模块 race 97 包 + tools 4 + tui 9 全绿。

---

## refactor(tools): 阶段4：死代码与弃用 API 清理（tool_domains/命令处理器/测试引用函数/域过滤函数）

按 docs/specs/tech-debt-fix/04-tasks.md 阶段 4 执行：T4.1 删除 tools/tool_domains.go 弃用别名文件（零引用，role.go 注释改指 agentcore.ToolDomains）；T4.2 删除 handleLedgerCommand/handleSessionsCommand（已由 EvidenceOverlay/会话选择器替代）+ 3 个专属测试；T4.3 删除 readFileSandboxed（测试改用 OpenSandboxed 直接验证）、resolveOCRCacheDir；T4.4 决策：保留 agentcore.ToolDomains 映射表（数据资产，角色工具过滤未接线需另立 spec），删除 ToolDomain/AllDomains/FilterToolNames/ToolHasDomain 4 个零调用函数。验证：四模块 lint 0 issues、根模块 race 97 包 + tools 4 + tui 9 全绿。

---

## refactor(cmd): 阶段5：重复代码消除（runPatentPipeline/queryRecords/slash 去重）+ 全部 5 阶段完成

按 docs/specs/tech-debt-fix/04-tasks.md 阶段 5 执行：T5.1 抽取 runPatentPipeline 统一 4 个 patent CLI run 函数（parse→build→run→output 骨架）；T5.2 slash 处理器 4 处 dupl 豁免中 2 处真实消除（oa/reexamination），2 处带理由保留（novelty/invalidation 的 GraphOption 类型不同）；T5.3 抽取 queryRecords 统一 approval_store List/ListByCase（WHERE 子句参数化）。至此技术债务修复 5 阶段全部完成（阶段1 安全测试补齐/阶段2 门禁统一/阶段3 复杂度拆分/阶段4 死代码清理/阶段5 重复代码消除）。验证：四模块 lint 0 issues、go vet/build 全过、根模块 race 97 包 + tools 4 + tui 9 全绿。

---

## test(*): 代码审阅：修复 graph 条件边错误吞没回归 + 简化 compProvider/repetitionState + 补 pipeline 契约测试

按 code-simplifier + requesting-code-review 技能审阅 5 阶段全部改动。修复：1) graph.applyNodeOutputs 条件边目标节点 Run 失败被 continue 静默吞掉（原实现 return error），改回错误传播；2) compProviderFor/compModelFor 去除冗余 fallback 参数，runCompaction 删多余解包；3) detectRepetition/runToolCallTurn 的 4 指针参数封装为 repetitionState struct；4) runPatentPipeline 重构补 5 个契约测试（打印/保存/缺输入/build 错/save 错）。确认 runCompaction 的 len(turnsToSummarize)==0 检查移除等价、eventToChat nil 防护正确、runPatentPipeline 文案统一非功能变化。
