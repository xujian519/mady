# 2026-08-02

## fix(tui): TUI 渲染小瑕疵修复：工具组 summary 双空格/工具卡窄宽/数学表达式保护/表头 inline/resize 提示对齐

按审查结论修复 TUI 输出/渲染组件 6 项 P2/P3 瑕疵：

改动清单：
1. chat_history_render.go — renderToolGroup summary 双空格：marker 去掉尾随空格，格式串统一补一个空格（'[+]  2 tools' → '[+] 2 tools'）
2. tool_card.go — renderToolCardHeader 窄宽保护：available 保底 1 列保留省略号语义；prefix+meta 超宽时整体截断，不再依赖引擎 normalizeLine 兜底
3. markdown_render.go — stripUnpairedMarker 数学表达式保护：两侧为数字的 2*3 / 4**2 保留；单字符 marker 紧邻同字符时交由 ** 分支处理
4. markdown_render.go — renderTableVertical 表头先过 renderInline，**bold** 不再裸泄漏
5. tui_render.go — renderResizeHint 按 core.VisibleWidth 计算 box 内宽并手动填充，修复 CJK 字节填充导致的右边框错位

回归测试：
- chat_history_render_extra_test.go：summary single-space format 精确断言（折叠无 Meta/带 Meta/展开）
- tool_card_test.go：TestToolCardHeaderNarrowWidth（极窄 10/15/20 宽）
- markdown_test.go：TestMarkdownParagraphKeepsMathExpressions、TestMarkdownTableVerticalHeaderInline

已知边界：renderInline 的 italic 正则仍可能把同一段落内两个数学表达式的星号误配成对（既有问题，本次未改动，已在测试注释中标注）。

验证：cd tui && go test -race ./... 全绿（9 包）、go vet ./... 通过、根模块 go build ./... 通过、tui 模块构建通过。

---

## fix(tui): renderResizeHint 对齐公式完善（review 驱动）：boxWidth=VisibleWidth+6 + 对齐断言测试

code-review 复核发现 2026-08-02 条目的 renderResizeHint 修复存在对齐偏差并完善：

- boxWidth 须为 VisibleWidth(hint)+6（原 +4 少算两枚边框列），inner=boxWidth-6，使 EN/CN 内容行与边框行严格同宽（EN 行 pad=0、CN 行 pad 到 L）
- 新增 TestRenderResizeHintAlignment：三条互补断言（CN/EN 宽相等、EN 不 pad、恰好两内容行）锁住 inner==L 公式，防字节填充回归
- 验证：cd tui && go test -race ./... 全绿、go vet 通过、根模块/tui 构建通过

---

## feat(desktop): 桌面端工程基础：版本注入 + 窗口位置持久化 + 原生菜单 + 会话重命名/回收站

仿照 Reasonix 桌面端（DeepSeek-Reasonix-main-v2）调整的阶段 1 工程基础（方案见 docs/plans/desktop-reasonix-reference-plan.md）：

1. **版本注入**：desktopVersion 由 const 改 var，Makefile desktop-dmg/desktop-build-quick 注入 -X main.desktopVersion；SettingsPanel 关于区版本行改为展示 health().version（不再硬编码 0.1.0）。
2. **窗口位置持久化**（S-8 修复）：windowState 增加 X/Y；App.beforeClose 用 runtime.WindowGetSize/WindowGetPosition 自取并落盘，startup 恢复位置；loadWindowState 增加位置绝对值上限校验。
3. **macOS 原生菜单**：新增 desktop/menu.go（AppMenu + 文件/编辑/窗口），设置 ⌘, 发 app:open-settings 事件，退出 ⌘Q；前端 ChatView 监听打开设置面板。
4. **会话自定义标题**：session.AgentStore.RenameThread（Open+SetSessionName+flushAll）→ server.RenameThread → App.RenameThread 绑定 → 前端 renameThread。
5. **会话回收站**（软删除）：session.FileStore MoveToTrash/RestoreFromTrash/ListTrashed/PurgeTrashed（.trash 子目录文件移动，List 天然排除）；AgentStore 4 包装 + server/App 4 绑定 + 前端封装。

验证：desktop 模块 go build/test、session 包 -race、server 全量测试、前端 tsc 全通过；新增 TestFileStore_TrashLifecycle / TestAgentStore_RenameAndTrash。

---

## feat(desktop): 桌面端多标签（Go 侧状态机）第一步：Tab 模型/持久化/恢复/绑定

按用户决策（Go 侧 tabs 状态机提前，方案 docs/plans/desktop-reasonix-reference-plan.md 2.1 调研结论）落地阶段 2.1a：

- 新增 desktop/tabs.go：Tab 模型（ID/ThreadID/Title/CreatedAt/ActiveAt）+ tabStore（内存真相源 + ~/.mady/desktop-tabs.json 持久化 + 启动恢复 + 坏文件容错 + 最后一个标签保护 + 关闭激活标签时激活相邻）。
- 新增 desktop/tabs_app.go：ListTabs / ActiveTabID / CreateTab / CloseTab / ActivateTab 5 个绑定。
- app.go：App 挂载 tabs 字段，startup 恢复标签。
- 前端：backend.ts 新增 5 个封装（DesktopTab 类型）；wailsjs 契约同步（App.js/App.d.ts/models.ts 含 Tab 类型）。
- 测试：tabs_test.go 4 个用例（生命周期/持久化往返/坏文件容错/内存模式），desktop 模块 -race 全过。

后续：2.1b 绑定 ForTab 化（Chat/GetThread 按 tab 分派）、2.1c 前端 TabBar UI。

---

## feat(desktop): 桌面端阶段 2 UI 升级：历史面板回收站 / 上下文环形指示器 / 回合折叠 / Composer 草稿与历史

阶段 2 的 4 个独立 UI 子任务（方案 docs/plans/desktop-reasonix-reference-plan.md）：

1. **历史面板 + 回收站（2.2）**：Sidebar 会话 tab 新增回收站视图（TrashPanel）；删除会话改为软删除（useDeleteThread 改调 trashThread），回收站内可恢复/彻底删除（useTrashedThreads/useRestoreThread/usePurgeThread，threadKeys.trashed 查询键，与主列表互失效）。
2. **上下文用量环形指示器（2.3）**：新增 ContextWindowRing（SVG 圆环 + 百分比 + 悬停 tooltip），颜色分级与 UsageStrip 一致，接入 ChatView 标题栏。
3. **Transcript 回合分组/折叠（2.4）**：ChatView 按 user 消息划分回合，组头细条显示消息数/时间，可点击折叠（collapsedRounds Set，折叠时仅渲染组头，虚拟列表结构不变）。
4. **Composer 增强（2.5）**：会话草稿（localStorage 按 threadId 隔离，切换会话自动恢复）；发送历史 ↑/↓ 导航（每会话 50 条上限、相邻去重、IME 组合期不触发）。粘贴图片因需后端附件协议（ChatRequest 扩展 + 落盘 + 沙箱）延后单独设计。

验证：前端 vitest（86+14 全过）+ tsc 通过；desktop 模块 Go 测试通过。

---

## feat(desktop): 桌面端多标签闭环：ChatInTab 绑定按标签分派 + TabBar 标签栏

阶段 2.1b/2.1c（Go 侧状态机路径的接线层）：

1. **2.1b 绑定 ForTab 化**：server 新增公开 EnsureThreadID（空则创建会话并返回实际 ID，解决 server.Chat 值传递下调用方拿不到新会话 ID 的问题）；tabStore 新增 Get/SetThreadID；App.Chat 提取公共 startChatRun，新增 ChatInTab(tabID, req)——标签未关联会话时自动创建并写回 tab.ThreadID（持久化），消息始终作用于该标签会话。
2. **2.1c 前端 TabBar**：新增 stores/tabs.ts（Go tabs 的前端缓存 + loadTabs/createTab/closeTab/activateTab 动作）与 components/TabBar.tsx（标签列表 + 新建/切换/关闭，激活态高亮）；ChatView 标题栏接入 TabBar，激活标签切换时同步 chat store threadId 并加载历史（竞态守卫）或清空新会话；chatSlice.sendMessage 在激活标签存在时改走 chatInTab。

验证：前端 tsc + vitest（86+14 全过）；desktop 模块 go test -race 通过；新增 TestTabStore_GetAndSetThreadID。

---

## feat(desktop): 桌面端主题系统：主题包背景变量 + 主题画廊 + JSON 导入导出

阶段 3（仿照 Reasonix 主题系统，方案 docs/plans/desktop-reasonix-reference-plan.md D 节）：

1. **3.1 主题包结构扩展**：ThemePackVars 新增 background 字段（CSS background 简写，支持渐变）；4 套主题补 8 组 light/dark 背景（径向/线性渐变 + 主背景回退）；buildThemePackCSS 注入 --color-mady-app-background；globals.css body 背景改用该变量（未注入回退 bg-primary）。
2. **3.2 ThemeGallery 画廊**：新组件卡片网格（背景渐变预览 + 文本/强调色样本 + 名称描述 + 激活态），点击切换主题包；接入 SettingsPanel 外观区（主题模式切换下方）。
3. **3.3 导入/导出（简化版）**：导出当前 mode+pack 为 mady-theme.json（前端下载）；导入校验文件格式/已知包/合法模式后应用；纯前端实现，不引入 Go 绑定。

验证：前端 tsc + vitest（86+14）通过；desktop Go 测试通过。

---

## feat(desktop): 桌面端记忆面板：memory 三层系统绑定 + MemoryPanel + Sidebar 记忆页

阶段 4 进阶能力第一项（仿照 Reasonix MemoryPanel，方案 E 节）：

- Go 侧新增 desktop/memory_app.go：ListMemories（合并 user/session/long_term 三层、按更新时间倒序、上限保护）、RememberMemory（手动写入长期记忆）、ForgetMemory（按 ID 删除）、RecallMemories（语义检索，上限 50）；经 bootstrap.Context.MemoryManager 访问，未就绪时返回错误（前端静默降级）。
- 前端：backend.ts 4 个封装 + MemoryEntry/ScoredMemory 类型；stores/memories.ts（Query hooks：useMemories/useMemorySearch/useRememberMemory/useForgetMemory）；components/MemoryPanel.tsx（列表 + 层标签/重要性/时间 + 语义搜索 + 手动添加 + 删除）；Sidebar 新增「记忆」tab（Brain 图标）。
- wailsjs 契约同步（App.js/App.d.ts/models.ts 新增 memory 命名空间）。

验证：前端 tsc + vitest（86+14）通过；desktop go test -race 通过。

---

## fix(desktop): 修复未提交变更审阅发现的代码质量问题

基于未提交变更的全量代码质量审阅，修复以下问题：
- ContextWindowRing：CSS 变量名错误（--mady-* 应为 --color-mady-*）致颜色失效；levelColor 重复分支合并
- Composer：发送历史跨会话串数据（切换会话时重载 sendHistoryRef）
- chatSlice：ChatInTab 成功后补 loadTabs()，避免切回标签时误判为新标签导致会话在 UI 丢失
- tabs.Close：激活相邻标签行为与注释不一致，改为优先左侧
- ChatView：回合计数由逐回合 filter 改单遍 Map 统计（消除 O(n²)）；折叠状态 key 由消息索引改为回合首条消息 id（索引漂移不再串位）
- ThemeGallery：去除 as never 强转，改用 ThemePackId/ThemeMode
- app.go：shutdown 文档注释归位
- memory_app：改用运行时 ctx（新增 memoryCtx helper）
- server/desktop.go：抽 requireThreadStore helper 消除重复样板
- session_store：回收站 MoveToTrash/RestoreFromTrash 前检查目标存在性，防静默覆盖
验证：desktop/server/session 模块 go build + go vet + go test -race 全通过；前端 tsc --noEmit 与 eslint（0 error）通过。
