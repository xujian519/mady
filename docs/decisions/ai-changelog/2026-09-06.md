# 2026-09-06

## refactor(domains): M13 日卡：domains/rules + reasoning 审阅精炼（保守档）

**背景**：docs/code-review-plan.md M13 日卡（domains/rules + reasoning，含 sqlite/wiring）审阅后的精炼。全仓对该模块做一轮「审阅+精炼」，只做无行为变化清理。scanner 静默吞错告警 5 处，其中 2 处为真实降级分支缺意图注释，另 3 处为扫描器仅认 // 注释的误报（planner 两条 LLM 降级已有 slog.Warn+doc 表达意图、sqlite 一处属错误传播）。

**改动清单**：
- rules/evaluate.go：extractFirstNumber 的 strconv.ParseFloat 失败分支补「文本中无数值降级返回 nil」意图注释
- reasoning/wiring/confirmed_rule_writer.go：List 的 os.IsNotExist fail-open 分支补「目录尚不存在=无历史记录，按空列表处理」注释
- reasoning/sqlite/checkpoint_store.go：删除文件尾重复的 var _ reasoning.CheckpointStore 编译时检查（文件头 var block 已有同款接口断言）
- reasoning/sqlite/doc.go：修正过时包注释——示例签名即与实际 Save(ctx, *StageCheckpoint) 不符、CheckpointStore 类型名误（实际为 SQLiteCheckpointStore），与包内导出对齐

**验证**：包级 go test -race 全绿（rules/reasoning/sqlite/wiring）；make verify 全绿（lint/check-arch/doc-check/verify-layers/build/test-race，含 tui/desktop 三模块）。

**影响**：无行为变化（仅注释与冗余声明清理）；scanner 吞错 5→3；未触碰敏感路径与工具 inputSchema；P1 2 项长函数（Tools()/CompilePlanToGraph）登记待拆，P2 plan_compiler.go 两处 _ = g.AddEdge 登记不处理。
