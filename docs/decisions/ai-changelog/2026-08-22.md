# 2026-08-22

## refactor(architecture): 架构债修复：解除循环依赖与分层违规

依据 Brooks-Lint 架构审计(50/100)与 docs/plans/architecture-debt-fix-plan.md 执行：1) retrieval/domain/sqlite 定义 FTSStore 接口解除 knowledge↔retrieval 循环；2) case_index 族(8文件)迁至 domains/caseindex 子包，根包 SQLite 驱动依赖清零，别名桥接保持兼容；3) pkg/agentconfig 迁至 bootstrap/agentconfig(组装逻辑归位组合根)；4) tools/patent_eval 以 SlopAnalyzer 函数类型切断 domains/rules 依赖；5) evaluate/reasoning_pattern_coverage 改为 PatternSource 注入，领域侧提供 PatternInfos adapter；6) tools 独立模块上收进根模块，消除 root↔tools 双向 require(go.work 移除 ./tools)；7) deadline 族抽至 domains/deadline；8) reasoning_patterns 592行数据拆分至 reasoning_patterns_data.go；9) buildSlashRegistry 495行拆为编排+4分组函数。验证：make verify 四模块全过，grep 断言全部反向边为空。
