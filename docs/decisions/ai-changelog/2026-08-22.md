# 2026-08-22

## refactor(architecture): 架构债修复：解除循环依赖与分层违规

依据 Brooks-Lint 架构审计(50/100)与 docs/plans/architecture-debt-fix-plan.md 执行：1) retrieval/domain/sqlite 定义 FTSStore 接口解除 knowledge↔retrieval 循环；2) case_index 族(8文件)迁至 domains/caseindex 子包，根包 SQLite 驱动依赖清零，别名桥接保持兼容；3) pkg/agentconfig 迁至 bootstrap/agentconfig(组装逻辑归位组合根)；4) tools/patent_eval 以 SlopAnalyzer 函数类型切断 domains/rules 依赖；5) evaluate/reasoning_pattern_coverage 改为 PatternSource 注入，领域侧提供 PatternInfos adapter；6) tools 独立模块上收进根模块，消除 root↔tools 双向 require(go.work 移除 ./tools)；7) deadline 族抽至 domains/deadline；8) reasoning_patterns 592行数据拆分至 reasoning_patterns_data.go；9) buildSlashRegistry 495行拆为编排+4分组函数。验证：make verify 四模块全过，grep 断言全部反向边为空。

---

## refactor(domains): 修复架构债评审反馈：解耦 patent→evaluate 依赖、修正期限天数截断并补测试

背景：架构债修复提交（9dc9816）后评审发现两类问题——领域层反向依赖基础设施层、以及期限计算的天数截断偏差。

改动清单：
- domains/workflows/patent 移除对 evaluate（基础设施层）的 import 与 PatternInfos()，消除领域→基础设施的分层违规；推理模式覆盖率评估器保持领域无关，改由组装接缝经 PatternSource 注入数据。
- domains/deadline 将泛型命名 Type/Summary 改为 DeadlineType/DeadlineSummary，避免小包内泛型命名歧义。
- 修正 newDeadline 天数计算：改用 daysBetween 按整日历天差（两侧归一到各自日历日期的 UTC 零点）计算，避免本地零点与 UTC 解析偏移或小数天截断导致的'差一天'偏差。
- 新增 domains/deadline 计算器测试：类型集合（发明/实用新型/外观）、优先权窗口（12/6 个月）、状态边界（normal/urgent 30 天/overdue）、非日期占位排序、整日天差及 UTC/本地时区偏移回归。

验证：go build ./...（root/tui/desktop）通过；go vet ./... 通过；domains/deadline 8 项测试及 patent/evaluate/caseindex 测试全部通过。

影响：领域层不再依赖基础设施层；期限剩余天数更准确；新增测试守护日历天数边界。

---

## fix(ci,docs): 修复 main CI 门禁并清理失效 dependabot 条目（PR 合并前置）

背景：远程仓库 7 个 dependabot PR 全部卡死——main 最新提交（2c4016f49）CI 失败，/tools 的 dependabot 条目指向已删除的 tools/go.mod。

改动清单：
- docs：CLAUDE.md/CONTRIBUTING.md 移除已随 9dc9816 删除的 pkg/agentconfig 路径引用，doc-check 恢复通过。
- tui：terminal/terminal_linux.go 3 处 ioctl unsafe 调用标注 #nosec G103（Linux ioctl 标准惯用法），tui lint 恢复通过。
- ci：.github/dependabot.yml 移除已失效的 /tools gomod 条目（tools 模块已上收根模块），避免 weekly 扫描持续报错。

验证：make doc-check 通过；tui golangci-lint 0 issues；CI 全绿后合并 #40-43（x/image 0.45.0、pi 0.84.18、x/net 0.58.0、wails 2.15.0）。
