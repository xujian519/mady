# 2026-08-03

## fix(tui/chat): 修复TUI流式输出排版混乱：滚动条列恒定预留

背景：流式输出期间内容从'可容纳'变为'溢出'时，滚动条出现/消失让 renderWidth 在 width 与 width-1 之间切换，Markdown 段落按宽度换行导致已渲染行换行点全部错位，表现为输出时排版混乱、重开 TUI 后正常。\n\n改动：computeRenderWidth 改为滚动条启用时内容渲染宽度恒为 width-sbWidth（方案1），sbNow 仅表示本帧是否绘制滚动条轨道；cachedWidth 不再因滚动条切换触发缓存失效与全量重排。同步更新 padToWidth 注释并新增回归测试 TestChatHistoryReservedGutterWidthStable。\n\n验证：tui/chat 全部测试通过（含 -race）。
