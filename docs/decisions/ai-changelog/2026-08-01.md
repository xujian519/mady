# 2026-08-01

## refactor(bootstrap): 统一 BaseConfig 构造并清理 desktop lint 遗留问题

在修复 MaxTokens 配置传递 bug 的基础上进一步重构：1) 将 BaseConfig 构造逻辑提取为 bootstrap.NewBaseConfig()，统一 cli/server/acp/desktop 四个入口的 max_tokens 默认值与用户配置覆盖，避免各入口重复维护同一份配置；2) LoadFallbackConfig() 改为接受 *agentconfig.Config 参数，避免重复读取 MADY_CONFIG 文件；3) desktop 模块同步接入 NewBaseConfig，修复其同样存在的 MaxTokens 未传递问题；4) 顺手修复 desktop 模块两个既有 lint 失败：tray.go 的 osascript 改为通过 stdin + CommandContext 执行，app_skills.go 的 ListSkills 拆分为 collectSkillDirs / loadUniqueSkills / skillEntryPath 降低认知复杂度。验证：make verify（vet + golangci-lint + go-arch-lint + doc-check + race test）四个模块全过。

---

## test(cmd/mady): buildAgentConfig 集成测试：MaxTokens 保留不变量

在 cmd/mady/tui_session_config_test.go 新增 TestBuildAgentConfig_PreservesMaxTokens 集成测试。构造最小 tuiSession，令 frameworkContext.BaseConfig.MaxTokens 为 sentinel 值（12345），调用 buildAgentConfig() 后断言返回的 agentcore.Config.MaxTokens 仍等于该值。该测试把 'MaxTokens 在 TUI 会话装配过程中必须保留' 变成受保护的不变量，防止未来再次出现在 buildAgentConfig 中整体覆盖 ModelConfig 导致 max_tokens 被清零的回归。验证：go test ./... 与 make verify 全绿。

---

## chore(docs): 文档一致性检查防漂移加固（--update 自动同步 + git 口径 + CI 接入）

背景：make doc-check 曾因 CLAUDE.md 手写计数（~1001/463）与实际漂移失败（1a29081 引入错误声明、643239b 才修复）；且 AGENTS.md 计数检查因全角括号正则不匹配从未生效（死代码）。

改动清单：
1. scripts/check-doc-consistency.py：计数口径改为 git ls-files（排除 node_modules/vendor/未跟踪文件，结果可复现）；新增 --update 自动同步 CLAUDE.md/AGENTS.md 计数与更新时间戳；修复 AGENTS.md 全角括号正则失效 bug；'1500+ 个 Go 源文件'下限声明改为解析式检查
2. .github/workflows/ci.yml：新增 doc-check job（此前 doc-check 不在 CI，漂移无法被拦截）
3. .gitignore：cli-chat/wiki-import 裸规则锚定根路径（/cli-chat /wiki-import），消除对 example/ 同名示例目录的误伤
4. example/cli-chat、example/wiki-import 纳入版本控制（共 4 个 .go 文件，此前被 .gitignore 误伤从未提交，CLAUDE.md 声称的 10 个示例补齐）；integration_test.go 增加数据目录缺失时 t.Skip（防止 CI 无数据环境失败）
5. AGENTS.md：计数同步为 git 口径（1532 非测试 1019 + 测试 513），文件计数更新时间戳刷新为 2026-08-01，提示命令改为 git ls-files 口径

验证：make doc-check 通过；--update 模拟漂移自动修复验证通过（注入 9999/8888/7777 → 自动恢复为实际值）；无漂移时 --update 为 no-op；go build/vet/test ./example/... 通过（wiki-import 真实验证 0.294s）；make all 四模块全绿（42s）。

影响：doc-check 纳入 CI 后，文档计数/子命令数/目录树/manifest 漂移会在推送与 PR 时被拦截；本地漂移可用 python3 scripts/check-doc-consistency.py --update 一键同步。

---

## style(tui): TUI 输出紧凑时间线与 Markdown 可读性优化

针对截图中工具调用留白过多、Markdown 裸标记污染、表格 ASCII 边框破碎等可读性问题，按 mady-tui-standards.md 与 Bubble Tea/Ratatui/Aider/Claude Code 等社区 Agent TUI 最佳实践实施「紧凑时间线」方案。

改动清单：
- `tui/chat/chat_history_render.go`: 消息分隔符从轻到重统一为单空行/无色带/细点线；工具组展开后用左侧 `│` 色带连接成员，消除散落的卡片感。
- `tui/component/tool_card.go`: 工具卡片头强制单行，过长状态按可用宽度截断，避免错误信息撑爆布局。
- `tui/component/markdown_render.go`: 段落文本清理行内未闭合/不规范的 `#`、`**`、`*`、`~~` 等裸标记；表格水平渲染阈值增加 2 列安全边距，更早触发垂直 key/value 回退。
- 测试：`tui/component/markdown_test.go` 新增表格提前回退与 Markdown 伪影清理测试；`tui/component/tool_card_test.go` 新增单行头测试；`tui/chat/chat_history_render_extra_test.go` 同步新的分隔符契约并新增工具组色带测试；`tui/chat/chat_layout_mouse_test.go` 因密度提升需追加更多消息以保证溢出。

验证：`make verify` 通过（vet/build/race 覆盖根模块 + tools + tui + desktop 四个模块）。

评审修复（code review 6 条全部落实）：
- 缓存宽度碰撞：`cachedMessage` 增加 `width` 字段，命中时宽度不一致即重渲染——展开工具组以 `innerW` 渲染后不再可能复用全宽缓存行导致右溢出（原缓存仅按 `m.ID` 键控）。
- `renderMessageSeparator` 从 7 个角色对枚举收敛为 4 分支（连续工具→无分隔；System↔System→空行；同角色→八分之一宽点线；其余角色切换→空行），并顺带统一 Assistant→System / User→Tool 为角色切换语义。
- `renderToolGroup` summary 构建去重：折叠/展开仅差 `[+]`/`[-]` 标记，统一用 marker 变量构建，删除重复分支。
- `reInlineHashes` 收紧为 `#{2,6}`：不再误删 `C#`/`F#`/专利图纸引用 `1#` 等合法单井号；注释同步修正（行首井号由块解析器归类为标题，正则只管行内残留）。
- 测试修正：`TestRenderToolGroup` 修复重复的相同条件（复制粘贴残留）并强化色带断言（2 成员 + 1 连接 = 3 处）；新增 `TestRenderToolGroupCacheWidthMismatch`（缓存宽度碰撞回归）与 `TestMarkdownParagraphKeepsSingleHash`（单井号保留回归）。
- 次要项：`TestHandleMouseMsgMissFallsBackToLegacy` 改为按渲染行数校准溢出（不再依赖 10→40 魔数消息条数）；`renderToolCardHeader` 宽度钳制从 6 收紧为 0，极窄视口下不再强制超宽。
