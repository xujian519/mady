# 2026-08-07

## refactor(techdebt): 技术债务清理：移除孤儿代码与仓库卫生修复（P0+P1）

背景：全量审计发现 6 个孤岛库包、189 个零引用导出符号、过期 vendor 目录与误提交二进制。\n改动清单：\n- P0: git rm --cached a2ui-demo（6.1MB 二进制曾入版本库）；删除过期 vendor/（158MB，modules.txt 与 go.mod 不一致，-mod=vendor 必报错）\n- P1: 删除 131 个全仓库零引用导出符号（含 agentcore/config.go 53 个死 With* 配置项，v0.4.0 Agent 三合一后旧配置 API 整体弃用）；连带清理删除后暴露的 12 处未导出死代码（bestStyleForDomain/globalPatentRoleSet/applyRoleConfig/domainInList/customLevels/yamlFSReadFile/defaultConstitutionalYAML/WorkflowOrchestrator 等）\n- 整体删除两个死文件：knowledge/unified_search.go（488 行）、mcp/mcp_server.go（600 行，含 var _ = strings.TrimSpace 抑制 hack）\n- 保留 a2a/acp/a2ui 协议规范 API 与 41 个被 cmd/mady、tui、desktop 实际引用的符号\n- gofmt 修复 agentcore/tool_domains.go 与 tools/desktop/computer_use.go\n验证：go build ./... + go vet ./... + golangci-lint（0 issues）+ go test -race ./...（98 包全过）+ 四模块子模块编译。净删除约 2600 行。

---

## refactor(techdebt): P2 孤岛模块处置：接线 intent、删除 5 个死副本

背景：对 6 个孤岛包逐一处置，以接线为主。调研发现其中 4 个是 root domains 包的重复副本（深度重构时复制到子包但从未接线），1 个能力已被活实现覆盖，仅 intent 是真正可接线的独立能力。\n处置清单：\n- 接线 intent：domains.ClassifyIntent 委托 intent.NewUnifiedRouter(NewKeywordClassifier())（原手工 intentrules 循环去重）；清理 intent 包内死文件（semantic.go/preference.go/hook.go，均无构造器、零外部引用）+ 对应测试\n- 删除 domains/casemgmt（root domains/case_*.go 完全重复且已接线）\n- 删除 domains/citation（root domains/citation_wiring.go 完全重复且由 bootstrap 注入）\n- 删除 domains/approval（root domains/approval*.go 完全重复，含状态机）\n- 删除 workflows（能力由 agentcore.OrchestrationManifest + reasoning.WorkflowManifest 活实现覆盖，ToFiveStepRunner 为占位）\n- 删除 graph/orchestrator（无消费场景，检查点 resume 需架构级改造）\n- 更新 CLAUDE.md 目录结构（移除 casemgmt/workflows 根包条目）\n验证：go build + go vet + golangci-lint（0 issues）+ go test ./...（94 包全过）+ 子模块编译。6 个孤岛包处置完毕。

---

## chore(techdebt): P3 仓库卫生收尾：清除根目录杂散文档与编译二进制

背景：P0-P2 遗留的仓库卫生项收尾。\n改动清单：\n- git rm 12 个一次性产物：architecture.html / bench-knowledge.txt / tui_doc_vs_code_analysis.md / overview.md / QUALITY_REVIEW_PLAN.md / QUALITY_REVIEW_REPORT.md 及 6 个根目录脚本（build_root.sh / exec_all.sh / exec_build_check.sh / run_all_checks.sh / run_build_check.sh / cleanup_temp.sh）\n- 保留 reasonix.toml（Reasonix 工具活跃配置，docs/plans/desktop-reasonix-reference-plan.md 关联）\n- 删除 5 个编译二进制：acp-server(24M) / cli-chat(17M) / mady(37M) / mady.test(30M) / a2ui-demo(6.1M，P0 已停止跟踪，本次清本地文件)\n验证：go build ./... 通过。P3 剩余 gofmt 修复已在 P1 完成（agentcore/tool_domains.go + tools/desktop/computer_use.go）。至此 P0-P3 全部完成。
