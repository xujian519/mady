# 2026-08-13

## feat(tools): 新增 spawn_agent 工具：基于 sky-valley/pi 的辅助智能体子会话

背景：Mady 缺动态子会话能力（Handoff 为静态目标，orchestration/worker 均在同一上下文内）。参照 Sati 子代理体系（agent 工具 + builtinSubagentTypes），引入纯 Go 框架 github.com/sky-valley/pi v0.84.17 作为子会话运行时。

改动清单：
- tools/piagent/ 新包：schema 转换（JSON Schema → pi ai.Schema）、工具桥接（agentcore.Tool → pi AgentTool，安全包装：只读拒绝/Policy 门控）、预设（explore/verify/plan/general-purpose，对齐 Sati）、spawn_agent 会话运行与结构化报告聚合
- tools/tools.go：ExtensionConfig.SpawnAgent 门控注册 spawn_agent 工具（L3 敏感路径，人工审阅）
- 模型解析：spawn_agent.model 参数 → DefaultModel → 环境变量（PROVIDER/MODEL/BASE_URL/API_KEY，与主 Agent 同约定）；超时 120s 可配 SPAWN_AGENT_TIMEOUT
- 依赖：pi 仅引入 x/image/x/text（tools 已有更高版本），零新增传递依赖，pin v0.84.17
- docs/specs/pi-subagent/ 四阶段文档 + README/specs 索引同步

验证：tools/piagent 单元+集成测试全绿（faux provider 确定性脚本覆盖 AC-1..7：结构化报告/只读拒绝/白名单/上下文隔离/用量上报/超时取消/模型降级报错）；tools 全模块 build+vet+test-race 通过；golangci-lint 零 issue；根模块 build 通过；文档一致性检查通过。

影响：新增可选工具 spawn_agent（默认不注册，需显式配置 ExtensionConfig.SpawnAgent）；不动 agentcore 主循环/Handoff/permission 语义。

---

## fix(tools): spawn_agent 代码审阅修复（API Key 解析/白名单排除/参数接线）

审阅发现的 6 项修复：
1. API Key 按 Mady 约定显式解析（API_KEY → DEEPSEEK_API_KEY 等 provider 特定变量）并传入 pi SessionOptions.APIKey，修复仅设 API_KEY 时子会话 401
2. general-purpose + exclude_tools 显式排除后回落到「继承全量」分支的历史 bug（白名单集合运算），新增回归测试
3. max_tokens 参数接线到 SessionOptions.MaxTokens（此前解析但未使用）
4. model 参数 :thinking 后缀（如 deepseek/deepseek-v4-flash:high）现在生效并优先于 thinking 参数
5. 移除 result.ErrorMessage 死代码分支与 PresetDefault 死常量
6. spawn_agent 注册顺序调整：工具快照现包含 ExtraTools 域工具

验证：make verify 全绿；新增 TestResolveAllowed_GeneralPurposeExcludeAll / TestResolveModel_ThinkingSuffix / TestResolveAPIKey_MadyConvention；golangci-lint 零 issue（含 unconvert）。

---

## docs(desktop): 桌面端社区规范与流程调研报告

新增 docs/plans/desktop-community-research.md：调研 Wails/Tauri/Electron 生态与开源案例（RiftShare/Portmaster/opskat）的桌面开发流程共识（架构分层/版本单源/tag 驱动 CI 发布/签名公证/自动更新/打包资产/UX 检查表），对照 Mady 现状给出 P0-P2 差距与行动顺序，与 mady-desktop-standards.md 流程维度互补。

---

## fix(desktop): 补齐桌面端 CI 前端门禁（vite 生产构建 + make desktop-test 聚合）

ci.yml desktop job 补 pnpm build 生产构建门禁（捕获 vite/rollup 配置、manualChunks、copyCmapsPlugin 产物问题）；Makefile desktop-test 由纯 Go 测试扩展为 Go 测试 + pnpm typecheck/lint/test/build 四门禁，闭合 M-DSK-TST-006 并同步调研报告 R-P1-1 状态。

---

## feat(desktop): 新增桌面端发布流水线 desktop-release.yml（R-P1-2）

desktop-v* tag 触发：matrix 构建（macOS universal / Windows amd64+NSIS，wails v2.13.0 固定版本）→ 凭据驱动的签名公证（macOS 复用 Makefile notarytool 链路，Windows signtool 条件步骤）→ 制品上传 → release job 建 GitHub Release 草稿 + SHA256 校验清单。版本单源取 wails.json productVersion 并 ldflags 注入。actionlint 校验通过；未配置签名凭据时自动跳过不阻塞。与 CLI 发布 v* 的 release.yml tag 前缀隔离。同步调研报告 R-P1-2/R-P1-3 状态。

---

## feat(desktop): 补齐 macOS 签名公证 entitlements 与桌面端版本单源脚本（R-P1-4/R-P1-5）

新增 desktop/build/darwin/entitlements.plist：hardened runtime + WKWebView JIT 三例外（allow-jit / unsigned-executable-memory / disable-library-validation），明确非沙箱（与任意工作区访问兼容）；Makefile desktop-notarize codesign 接 --entitlements。新增 scripts/bump-desktop-version.sh：wails.json 为版本权威源，联动 app_update.go 默认值与 frontend/package.json（Info.plist 由 Wails 模板生成免手改），双向实测通过。

---

## feat(desktop): 自动更新阶段一：真实版本检测 + 手动下载引导（R-P1-6）

app_update.go CheckUpdate 由占位升级：查询 GitHub Releases（desktop-v* tag，跳过 CLI v* 发布），semver 比较，返回 hasUpdate/downloadUrl（zip asset 优先，回退 Release 页）；网络失败返回错误不误报已是最新。新增 app_update_test.go 10 个用例（httptest mock）。重新生成 wailsjs（UpdateInfo 增 downloadUrl）；SettingsPanel 发现新版本时展示内联「打开下载页」按钮（BrowserOpenURL http/https 白名单）。自替换后置至公证落地（阶段二）。typecheck/lint/vitest/契约/race 全绿。

---

## chore(desktop): 加固桌面端发布流水线签名门禁并本地验证 macOS 构建链路（R-P1-3）

desktop-release.yml：macOS 公证步骤门禁收紧（p12 证书 + Apple ID 同时存在才执行，缺证书跳过避免误导性 codesign 失败）；Windows 签名后补 signtool verify /pa 校验步骤。本地端到端验证 wails build darwin/arm64 + ldflags 版本注入 + 打包 + 自签名全链路（14.5s，产物含注入版本串）。剩余 P1 工作均依赖真实 Developer ID 证书配置（外部凭证）。

---

## fix(desktop): 代码评审修复：预发布过滤 / URL 解析容错 / 版本脚本替换校验 / 测试补齐

多角度评审发现的 4 处修复：(1) app_update.go ghRelease 捕获 prerelease 字段并跳过预发布（desktop-v*-beta 不再被推送给用户误当正式版）+ 补测试；(2) SettingsPanel handleOpenDownload 的 new URL() 包 try/catch 防畸形地址抛未捕获异常；(3) bump-desktop-version.sh sed 静默失败风险——逐文件替换校验；(4) updateDownloadURL 无 zip 回退 Release 页补测试。typecheck/lint/vitest/契约/race 全绿。
