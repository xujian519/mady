# 2026-08-11

## feat(tui): TUI P0 优化实施：补间动画 + OSC8 链接 + Golden 快照 + 能力接口

背景：Go 生态 Agent TUI 对标调研（docs/design/go-tui-ecosystem-benchmark.md）识别出 4 项 P0 差距，按实施方案（docs/design/go-tui-p0-implementation-plan.md）落地。

改动清单：
1. 补间动画：core/tween.go（Easing 6 种预置 + Tween 纯函数）；Overlay 打开动画（SlideUp 底部滑入 / Fade 毛玻璃渐显），Overlay.Transition 零值=无动画向后兼容，链式 Tick 驱动自终止
2. OSC 8 超链接：core/link.go（LinkSpan/LinkProvider 受信任通道，ParseLine 不解析链接标记，LLM 原始 OSC 8 剥离边界不变）；Row.Links 元数据 + SerializeRow 注入 + RowsEqual 链接比较 + 含链接行全行重写；NewTUI 按终端能力自动开关；evidence_card 引用项接入（EvidenceRef.URL），ChatHistory 渲染管线贯穿 links（含 splice fast path 与视口裁剪同步）
3. Golden 快照：component/golden_test.go（TestMain 固定调色板 + 4 卡片 × 3 宽度 × 折叠矩阵 18 个基线文件，GOLDEN_UPDATE=1 更新）
4. 能力接口：core Expandable/Compactable 可选接口；ChatMessage.IsCollapsible 统一折叠判定，click-to-toggle 收敛，evidence card 新增点击折叠能力

验证：cd tui && go test -race ./... 全绿；make verify 四模块 lint+build+test-race 通过；verify_layers.sh 同步（LAYERS.md 116→118 文件）。

影响：tui 模块新增 6 源文件 5 测试文件；Overlay/Row 结构体新增字段（零值向后兼容）；渲染管线返回值扩展（chat 包内部）。

---

## refactor(tui): 代码审查重构：链接元数据 nil 语义统一 + renderAllWithState 签名收敛

按 code-review 审查结论修复 5 项结构问题：
1) 统一链接元数据语义：renderMessage/renderDomainCard 各分支移除 nilLinks(len(...)) 样板，改为返回顶层 nil（无链接），由 renderMessagesRange 单一兜底补齐等长——消除顶层 nil 与逐行 nil 两种语义混用；
2) renderAllWithState 由 13 个散参数折叠为直接接收 renderSnapshot（唯一调用点 renderAllFromSnapshot 不再拆字段）；
3) renderMessagesRange 分隔行按 renderMessageSeparator 实际行数补齐链接元数据（修复 Tool↔Tool 返回 nil 分隔时破坏等长不变式的隐患）；
4) addScrollIndicator/addStickToBottomHint 无链接路径保持 nil，不再制造长度 1 的元数据；
5) Render 视口裁剪移除长度钳制，等长不变式显式化（fail-fast）；core.LinkProvider/LinkSpan 注释补充排序/不重叠/不跨行前置条件与 width 参数语义。
验证：make verify 四模块全绿；verify_layers.sh 通过（118 files）。行为不变（既有测试全过，无 golden 漂移）。

---

## fix(tui/chat): 修复折叠 Assistant 消息视觉截断错觉

视觉验证发现折叠 Assistant 消息用 WrapAnsi 软换行导致预览膨胀为 2-3 行+expand，视觉上被误以为输出截断；同时首行 200 阈值按字节截断 CJK 文本会切在多字节中间产生乱码。改两处：(1) WrapAnsi → TruncateToWidth，折叠态严格 2 行（单行预览+expand 提示），宽度溢出追加省略号；(2) len(bytes)>200 → len([]rune)>200，汉字按 rune 截断不生半字。补 3 个回归测试：2 行约束、CJK 无 RuneError、120 ASCII 中等长度不被软拆。验证 tui/chat/core/component race 全过，Desktop reducer 5/5 通过。

---

## feat(tui/chat): Reasonix TUI 对标：输入框圆角 + Todo锚定面板 + StatusBar密度

4项对标优化：1.editorFrame 品牌圆角╭─╮│╰─╯+顶框标题+底框操作提示；2.todoBar 锚定面板显前3活跃任务；3.Ghost Text 补↑↓历史提示；4.StatusBar 加P/C token/T#轮次/⏳retry芯片。测试tui/chat/component/layout/tui全模块race通过。

---

## fix(tui/chat): 修复 Reasonix 对标审查 4 项问题

code review 4 fix: (1) editorFrame.Render 删除 L69-L71 死代码 if tw>inner 分支（L59-L62 已截断，旧分支会覆盖 topBody 丢边框填充）。(2) todoBar.Render 改为先 TruncateToWidth(row,width-1) 再基于截断后 VisibleWidth 重新计算 fill 空格，避免长内容+ANSI 下边框不对齐。(3) onAgentError 对照 onAgentEnd 补 turnCompleted++(锁内)、SetTurn、SetRetry(0,0)，修复失败回合 turn 不递增 + retry chip 残留。(4) recalcMaxRows 新增 todoBarH := len(todoBar.Render(width)) 并入 reserved，WindowSize 触发时 history SetMaxRows(remaining) 与实际 flex 堆叠精确一致。验证：tui/chat/component/layout/core go test -race 全过。

---

## feat(domains): 引入 Sati 单一性评分引擎与技术问题四检验

**背景**：对照 Sati 仓库（TypeScript）移植两处差异化确定性能力，填补 Mady 专利域空白：单一性仅有 prompt 级条款（P-A08），技术问题校验无原子化规则。

**改动清单**：
1. `domains/claimdrafting/unity.go` 新增单一性评分引擎（特征相似度 = 0.2×字符重叠 + 0.5×Jaccard + 0.3×Bigram 余弦，阈值 0.6，评分 = 60 + 40×最弱对相似度，对齐 Sati unity.yaml 规范）；`unityInventionRule` 改用评分引擎并删除旧启发式。
2. `domains/claimdrafting/rules_support.go` 新增 `support-range-endpoint` 规则（数值范围端点实施例支持，CLA-001 第 3 条）。
3. `domains/inventiveness/problem.go` 新增技术问题四检验（不绑方案/单一因果/可测效果/手段可反推，对应审查指南 3.2.1.1），`Step2Result` 挂载 `problem_checks`。

**验证**：go build ./... && go vet && go test ./domains/... 全绿；新增 `problem_test.go`、`unity_test.go` 覆盖正反例。

**影响**：claimdrafting 输出新增单一性评分诊断；inventiveness 输出新增技术问题合规校验字段（JSON 后向兼容）。

---

## feat(domains): 引入 Sati 化学文本提取与附图一致性校验

对照 Sati 差异化资产（chemistry / figure）移植确定性逻辑到 Mady 领域层，并完成 skill 方法论资产的接线。

## 改动清单

1. **skills/patent/SKILL.md 接线**：将 Sati patent-agent 的 6 个 references 文档全部接入（五步工作法、质量门禁流程②自查清单、③HITL 节点），包括 legal-reasoning（五步三段论）、output-standards（法条引用/编号层级）、patent-glossary（术语口径）、quality-checklist（通用自查）、stop-slop（AI 套话审查）、hitl-protocol（确认点与中断指令）。

2. **domains/chemistry/**（新增包）：移植 Sati `src/patent/chemistry/text.ts` 文本级化学实体提取：
   - `IsValidHillFormula`：118 元素周期表校验 + Hill 记法，要求 ≥2 种真实元素（排除 C12/CD4/DNA 等噪声）；
   - `ExtractFormulaCandidates` / `ExtractSmilesCandidates`：分子式与类 SMILES 词元提取（Go RE2 无 lookbehind/lookahead，改为捕获 + 手工边界校验）；
   - `ExtractChemicalCandidates` 统一入口。含 16+ 测试用例。

3. **domains/figure/**（新增包）：移植 Sati `src/patent/figure/multi-figure-consistency.ts` 跨附图一致性核验：
   - 组件跨图四维度冲突（symbol/category/name/value）；
   - 图号连续性检查、图文对齐（claim 引用 vs 附图标记，符号库前缀过滤）；
   - 单引脚非电源孤立网络检测。纯函数、无模型依赖，可由 vision/ocr 流水线喂入。含 8+ 测试用例。

## 验证

- `go build ./...` 通过
- `go vet` 通过
- `golangci-lint run`（改动包）0 issues
- `go test -race`（chemistry + figure）全过

## 影响

无破坏性变更：全部为新增包/新增文件与 SKILL.md 文档接线。

---

## fix(domains): 修复 Sati 移植审查问题：泛指词排除位置/端点边界/死字面量

**背景**：代码审查（TRAE-code-review）对 Sati 移植改动发现 3 处与参考实现语义不符的确定性缺陷，按审查结论全部修复。

**改动清单**：
1. `domains/inventiveness/problem.go`：`checkNoSolutionBinding` 泛指手段词排除位置与 Sati 负向前瞻（位于"通过"之后、动词组之前）语义不符——旧逻辑排除"动词后内容前缀"，对"通过采用现有技术手段…"等句式误放行。改为逐"通过"位置判定：仅"通过+泛指词"直接连写形态豁免，动词组命中即视为绑方案；同步修正 `problem_test.go` 固化的相悖断言并拆分正反例测试。
2. `domains/claimdrafting/rules_support.go`：端点覆盖匹配由裸 `strings.Contains` 改为带词边界的 `endpointMentioned`（两侧不得为数字/小数点），避免端点 "10" 被说明书 "100mm" 等更长数值串误匹配（false positive 会掩盖缺失端点违规）；新增回归测试。
3. `domains/claimdrafting/unity.go`、`rules_support.go`：清除 "由…组成"（U+2026 省略号字面量）死字面量，统一收敛为有效停用词 "组成"（对齐 Sati unity.yaml 的 `由.*组成` 正则语义），修复真实文本永无法命中的缺陷。

**验证**：go build ./... 通过；go vet 通过；golangci-lint run（改动包）0 issues；go test -race（inventiveness + claimdrafting）全过。

**影响**：问题校验对"通过+动词+泛指词"句式收紧为绑方案；端点支撑判定更精确；相似度清洗新增 "组成" 停用词（评分可能有微小变化）。

---

## fix(tui/component): 修复编辑器 Tab 插入 NUL 与 Ctrl+Shift+Z 无法重做

视觉验证发现并修复两个键盘缺陷：\n1) Tab 在无 autocomplete 时执行 insertRune(k.Rune) 而 k.Rune 为 NUL（\x00），向 buffer 写入不可见控制字符污染提交文本；改为忽略。\n2) keysEqual 对单字符键忽略 Shift 修饰符，导致 ctrl+shift+z 同时匹配 undo(ctrl+z) 与 redo(ctrl+shift+z)，undo 分支在前使 redo 从键盘不可达；改为绑定显式声明 Shift 时要求实际按键带 Shift，并将 redo 分支移至 undo 之前。\n新增 editor_keys_dispatch_test.go（11 个键盘分发测试）覆盖 processKeys 层（原覆盖率 27.7%）。\n验证：go test ./... 全绿、-race 通过、pty 实测 Tab 输入不再含 NUL。

---

## fix(tui/chat): 修复 Ctrl+P 命令面板未接入与 Ctrl+C 空闲不退出

视觉验证发现的 footer 提示与实际行为不符问题：\n1) footer 提示 'Ctrl+P cmd' 但 chat 层 dispatchKey 无 ctrl+p 分支，命令面板从未打开。ChatAppConfig 新增 OnCommandCenter 回调，dispatchKey 转发 ctrl+p，cmd/mady 注入 openCommandCenter。\n2) footer 提示 'Ctrl+C quit' 但空闲时 Ctrl+C 被静默吞掉（editor.onCancel 死代码、无 QuitMsg 路径）。handleCopyOrInterrupt 空闲分支改触发 OnQuit（流式中仍走 OnInterrupt，语义不变；OnQuit 未配置时安全降级）。\n新增 3 个 chat 回归测试。\n验证：go test ./... 全绿、-race 通过、pty 实测 Ctrl+P 打开命令中心、Ctrl+C 退出进程。

---

## fix(cmd/mady): 修复默认主题 light 启动时误报未知主题

SettingsStore 默认主题为 light/dark（settings_store.go DefaultTheme），但主题注册表无这两个名字。applyStoredTheme 有 legacy 兜底，而 handleThemeCommand 缺失，导致每次启动执行 '/theme light' 都打印 '未知主题: light' 警告并出现在首屏。\nhandleThemeCommand 补 light/dark legacy 别名映射（与 applyStoredTheme 一致），持久化保持原值不变，非 auto 主题停止系统外观跟随。\n新增 3 个 cmd/mady 回归测试（别名应用/无效名仍警告/启动路径无警告）。\n验证：go test ./cmd/mady/ 全绿、pty 实测首帧无主题警告。

---

## refactor(cmd/mady): 收敛主题名→应用映射为 applyThemeByName 单一来源

代码审查发现：'主题名 → SemanticTheme' 的映射在 applyStoredTheme（启动路径）与 handleThemeCommand（交互路径）重复两份；handleThemeCommand 内部注册表分支与 legacy 分支还重复 4 步公共尾部（History 主题/持久化/确认消息/watcher 管理）。\n提取 applyThemeByName(name) (display, ok)：注册表名 + legacy light/dark 别名单一映射；applyStoredTheme 与 handleThemeCommand 共用。handleThemeCommand 两分支合并为单一路径，重复代码消除。\n另：editor Tab 分支 if 两臂均为 continue（死代码）简化为无条件 continue；Ctrl+P 分支补 super/meta 排除与 handleCopyOrInterrupt 一致；Ctrl+C 空闲降级显式化 default。\n验证：pty 实测 3 项行为不变、go test ./... 全绿、-race 通过。

---

## docs(repo): 修复 doc-check 漂移：同步 Go 文件计数声明

背景：2026-08-11 多个 push 触发的 CI run 在 doc-check job 失败——CLAUDE.md/AGENTS.md 中声明的 Go 文件计数与 git 实际跟踪数不一致（新增测试文件后未同步）。改动：运行 scripts/check-doc-consistency.py --update 同步 AGENTS.md（1531 个 Go 源文件：1001 非测试 + 530 测试）与 CLAUDE.md（~1001 非测试 + ~530 测试），并刷新文件计数更新时间戳。验证：doc-check 通过、go build ./... 通过、pre-commit hooks 全过。

---

## feat(tui/chat): 用户消息视觉区分：接线 UserPrefix/UserStyle 前缀渲染

背景：用户输入与智能体输出渲染无差别（均无前缀、无角色颜色），仅靠空行分隔，长会话中难以区分双方内容。根因：ChatHistoryTheme.UserPrefix/UserStyle/AssistantStyle 字段已定义但渲染层从未应用（renderMarkdownRole 注释明示 No left bar prefix）。

改动清单：
- chat_history_render_message.go：新增 renderUserRole，RoleUser 渲染应用 UserPrefix（默认 "> "，UserStyle 着色）+ 续行缩进对齐前缀宽度；前缀为空时退化为纯 markdown（向后兼容）；innerWidth 扣减前缀宽避免长消息溢出
- chat_history_user_prefix_test.go：新增 6 个回归测试（前缀/续行缩进/宽度不溢出/空前缀退化/自定义前缀/助手侧无前缀）

验证：tui 模块 go build ./... + go vet + go test ./... 全绿；chat 包 go test -race 通过；根模块 go build ./... 通过；gofmt 干净。未改动默认前缀值（"> "），可配置为 "❯ " 等。

影响：仅 tui/chat 渲染层；选区高亮按列覆盖 cell 样式自动纳入前缀列，无需偏移；链接元数据（用户消息恒 nil）不受影响。

---

## fix(retrieval): e2e 测试门禁修复——MADY_E2E 显式门控，make verify 不再被外部 ego-browser 依赖卡死

## 背景
make verify 门禁被 retrieval/domain/browser 的 e2e 测试卡死：这些测试真实调用 ego-browser + 外部专利数据库（Google Patents/CNIPA/Espacenet），t.Skip 仅检测二进制存在（IsAvailable 只查 EgoBrowserPath 非空），不探测运行时可用性；本机有二进制但浏览器运行时超时（Runtime.evaluate timed out）→ make verify 失败（151s 超时）。

## 改动清单
1. `retrieval/domain/browser/e2e_test.go`：新增 skipUnlessE2E helper（MADY_E2E=1 显式门控 + IsAvailable 二道闸），4 个 e2e 测试（Google/CNIPA/Espacenet/GetDocument）接入
2. `tools/e2e_tool_test.go`：TestE2EPatentWebSearchTool 加 MADY_E2E=1 门控
3. `Makefile`：新增 test-e2e 目标（MADY_E2E=1 显式运行 e2e），纳入 .PHONY

## 验证
- 默认（无 env）：5 个 e2e 测试全部 SKIP（0.00s）
- MADY_E2E=1：真实调用 ego-browser（61s 后失败，证明门控分支正确、不再误跳过）
- browser / tools 包全量测试 PASS（mock 路径无回归）
- make verify 全绿（63s，Exit 0）

## 影响
e2e 测试默认跳过，需 MADY_E2E=1 或 make test-e2e 显式运行；生产代码零改动（仅测试文件 + Makefile）。

---

## refactor(agentcore): 死代码清理：删除 iface.AgentRunner 死接口与 provider/adapter 功能孤岛

## 背景
中道审阅批 1：清理两处确凿死代码（均经删除前零引用验证，含测试）。

## 改动清单
1. `agentcore/iface/agent.go` 整文件删除：AgentRunner 接口零实现零消费（全仓仅定义处与 doc.go 注释引用），且签名与 agentcore.Agent.State()（返回指针）编译期不匹配；AgentState/AgentStatus 为同名重复定义（agentcore/state.go 已有真实类型）
2. `agentcore/iface/contract_test.go`：删除 TestAgentStateValues/TestStatusConstants（保留 TestInterfaceContract）
3. `agentcore/iface/doc.go`：移除 AgentRunner 契约描述
4. `provider/adapter/` 整目录删除（adapter/claude/codex/session + 测试，共 454 行）：全仓唯一引用是 cmd/mady/main.go:48 blank import，注册表从未被读取（LookupAdapter/ListAdapters/DetectAll/AdapterIndex 零消费者）
5. `cmd/mady/main.go`：移除 blank import

## 验证
- 全仓零残留引用（grep AgentRunner/provider/adapter 无匹配）
- gofmt 干净；go build ./... 通过
- agentcore/iface、agentcore、provider/... 测试全过
- make verify 全绿

## 影响
无功能影响（均为零引用死代码）。如需 adapter 能力（外部 Agent CLI 适配），git 历史可回溯。

---

## refactor(agentcore): 抽象收敛：删除 iface.EventBus 适配链，server 直连 agentcore.EventBus

## 背景
中道审阅批 2：iface.EventBus 适配链（agentcore.EventBus → eventBusAdapter → iface.EventBus，Event 类型往返转换）仅 server 一个消费者，而 server 本就直接依赖 agentcore（chat.go/desktop.go 已直接用 agentcore.Event），适配层价值有限。

## 改动清单
1. server 侧直连（7 文件）：server.go 字段/创建/On/OnAll/EmitEvent/EventBus 方法签名从 iface 类型改为 agentcore 类型；disclosure.go/disclosure_events.go/enablement_events.go 的 bus 参数与 onEvent 回调改 agentcore.Event（DisclosureCompletedEvent 已实现 agentcore.Event，直接断言，去掉 payload 包装）；skills.go 的 chan/On/EmitEvent 简化（SkillsReloadedEvent 直接 Emit，去掉 iface.NewEvent payload 包装）；cmd/mady/server.go OnAll 回调简化（e.Payload().(agentcore.Event) 冗余断言消除）
2. 删除 iface 事件层（3 文件）：agentcore/iface/event.go 整文件删除（Event/EventType/EventHandler/EventBus/SimpleEvent/NewEvent）；iface_adapter.go 删除 eventBusAdapter/NewIFaceEventBus/wrapIFaceEvent/ifaceWrappedEvent/payloadEvent（保留 ifaceLifecycleHookAdapter）；doc.go 更新契约与历史说明

## 验证
- go build ./... 通过；gofmt 干净
- agentcore/iface、agentcore、server 包测试全过（TestServerSkillReloadEmitsEvent 修正为指针断言：NewSkillsReloadedEvent 返回 *SkillsReloadedEvent）
- 全仓 iface 事件符号零代码引用（仅 doc.go 历史注释）
- make verify 全绿

## 影响
无功能变化：事件语义等价（此前 payload 包装的就是事件本身，现在直接 Emit 事件）；server 公共 API On/OnAll/EmitEvent/EventBus 签名从 iface 类型改为 agentcore 类型（外部调用方 cmd/mady 已同步）。

---

## refactor(domains): 规则引擎收敛：抽取 domains/rulekit 公共骨架 + evidence 数据归位

## 背景
中道审阅批 3：claimdrafting 与 specdrafting 各自实现一份 RuleEngine（注册/批量校验/按严重度分组）与 baseRule 骨架（约 170 行重复）；specdrafting 为复用 Severity 而耦合 claimdrafting（不健康依赖）；evidence 的 YAML 数据文件寄生在 domains/rules 目录（数据与解析器跨包错位）。

## 改动清单
1. 新建 `domains/rulekit/`（公共骨架）：Severity 类型与常量、Violation（两领域通用字段 + ClaimNumber/SectionName 可选字段，omitempty）、泛型 Rule[T,C] 接口与 Engine[T,C] 引擎（Register/RegisterAll/Rules/Validate/ValidateAndGroup）、BaseRule/NewBaseRule、ContainsAny 词表工具
2. claimdrafting（types.go/rules.go + 6 个规则文件）：Severity/Violation 改 rulekit 类型别名；ClaimRule/RuleEngine 改 rulekit 泛型别名；规则 struct 嵌入 baseRule → rulekit.BaseRule（32 处）；词表函数委托 rulekit.ContainsAny
3. specdrafting（同流程，5 个规则文件）：同上；**解除对 claimdrafting 的 import 依赖**（Severity 改从 rulekit 复用）
4. evidence 数据归位：`domains/rules/data/rules/evidence-rules.yaml` 移到 `domains/evidence/data/`；新增 `domains/evidence/embed.go`（go:embed + EvidenceRulesYAML）；删除 `domains/rules/embed_evidence.go`；bootstrap/setup.go 改从 evidence 包取数据

## 验证
- go build ./... 通过；gofmt/vet 干净
- claimdrafting/specdrafting/evidence/rules/bootstrap 包测试全过（Violation 合并后 JSON 字段 claim_number 加 omitempty，无测试断言受影响）
- make verify 全绿

## 影响
行为等价：引擎语义与规则数据完全不变（Violation JSON 中 claim_number:0 不再输出，语义更优且无断言依赖）；specdrafting 不再依赖 claimdrafting（依赖倒置到公共 rulekit）。

---

## refactor(domains): 图引擎装配收敛：analysiskit 共享法条框架骨架 + AssemblePregel 装配器

## 背景
中道审阅批 4：novelty/inventiveness/enablement 三份 framework.go 的骨架（ArticleFrameworkProvider/Data/StepData/Framework/NewFramework/GetArticleFramework/formatArticleData，~80 行×3）逐字重复，差异仅在法条 id 与降级默认文本；6 个分析模块的 Pregel 装配样板（AddNode 循环 + AddEdge 循环 + Compile，~20 行×6）同构。

## 改动清单
1. 新建 `domains/analysiskit/`：法条框架骨架（ArticleFrameworkProvider 接口 + Framework 查询器，注入式 ids/fallback 设计 + FormatArticleData）+ AssemblePregel 装配器（AddNode/AddEdge 循环，不 Compile，留给调用方追加条件边）
2. 三模块 framework.go：删骨架，保留领域知识数据函数（defaultA222/223/263Framework、inventiveness 的 8 个发明类型函数 + EmpiricalStatistics——这些是测试守护的领域知识库，非死代码）；类型别名 + NewFramework 包装（ids/fallback 注入）保持 API 兼容
3. 6 模块 graph.go：装配循环改 analysiskit.AssemblePregel（novelty 的条件边 SetConditionalEdge 在 AssemblePregel 后追加，拓扑不变）

## 验证
- 全仓构建通过；gofmt 干净
- 6 模块 + analysiskit 测试全过（TestFormatArticleData 改测 analysiskit.FormatArticleData）
- 图拓扑与错误消息语义不变（AssemblePregel 保留模块名前缀错误包装）
- make verify 全绿

## 影响
行为等价：框架查询语义与图拓扑完全不变；inventiveness 的领域知识函数全部保留（测试守护）；infringement 的 framework.go（不同设计，3 方法接口 + 默认常量）不在合并范围，保持原样。

---

## refactor(domains): Agent 配置装配收敛：assemble.go 共享四份装配块 + 修复 provision 列表重复拼接

## 背景
中道审阅批 5：PatentAgentConfig / LegalAgentConfig / UnifiedAgentConfig / BuildProjectAgent 四份装配块存在 40-55% 逐行重复——inject 五连（Drafting/DocTemplate/Prompt/Writing/Rules）+ 知识库/证据扩展注入（3 处）、DoomLoop + Gateway 统一决策入口（4 处）、LevelStandard 引用核验门（3 处）、LevelStrict 护栏 + 人工审批门（3 处）、claimdrafting/checker/specdrafting/Orchestration 撰写扩展（2 处）。另发现 patent.go System Prompt 中 provisions.BuildProvisionListForSystemPrompt(manifest) 连续调用两次（复制粘贴 bug，法条列表在系统提示中重复拼接）。

## 改动清单
1. 新建 domains/assemble.go：5 个共享组合函数——injectDomainSharedExtensions（五连 + 知识/证据扩展）、appendDraftingExtensions（撰写类扩展）、appendGatewayLifecycle（DoomLoop + Gateway，modify 参数供 UnifiedAgentConfig 挂 gatewayModifier）、appendStandardCitationGate（P1b Standard 核验门）、appendStrictGuardrails（LevelStrict 护栏 + 审批门，domain/disclaimer/withDQ 参数化 Patent/Legal/Project 差异）
2. domains/patent.go：PatentAgentConfig 与 BuildProjectAgent 装配块改调共享函数；删除 checker import；修复 BuildProvisionListForSystemPrompt 重复调用
3. domains/legal.go：装配块改调共享函数（inject 顺序统一为 patent 序，Tools=[drafting,docTemplate,prompt]、Extensions=[rules,writing] 最终顺序不变）
4. domains/unified.go：DoomLoop+Gateway 段与 Standard 核验门改调共享函数（gatewayModifier 语义保留）

## 验证
- go build ./domains/... 通过；gofmt 干净
- make verify 全绿（lint + check-arch + doc-check + build + test-race 四模块，66 秒）
- 敏感路径处理：patent.go（BuildProjectAgent 动态 WorkingDir）与 project.go（ValidateProjectPath）的安全语义代码未改动，仅装配段引用共享函数

## 影响
行为等价：各装配块最终 Tools/Extensions/Lifecycle 追加顺序与内容不变（legal 的 inject 顺序差异经核实最终结果一致）；patent 的 P2b 引用核验门（newCitationGate 留痕）保持专利独有，未并入 Standard 版本；unified 的 LevelLight 护栏保持原地；System Prompt 修复后法条列表不再重复（原为输出瑕疵，无功能依赖）。

---

## refactor(knowledge): RRF 融合三处合一：retrieval.FuseRRF 便捷函数统一调用点

## 背景
中道审阅批 6：RRF 融合调用（NewRRFFuser + Fuse 样板）在 knowledge 三处重复——KnowledgeExtension.backendSearch（含 rerank 分支）、sqlite.WritableStore.Search、fileindex.FileIndex.Search（构造时预建 rrf 字段）。三处均使用默认 k=60，无自定义 K。

## 改动清单
1. retrieval/hybrid.go：新增包级便捷函数 FuseRRF(lists, topK)，等价于 NewRRFFuser().Fuse（RRFFuser.Fuse 为无状态纯函数，仅依赖 K 字段）
2. knowledge/extension.go：backendSearch 删 fuser 局部变量，rerank 分支（candidateK）与非 rerank 分支（topK）均改调 retrieval.FuseRRF
3. knowledge/sqlite/writable.go：Search 的融合调用改 retrieval.FuseRRF
4. knowledge/fileindex/store.go：删除 FileIndex.rrf 字段（构造处 + 调用处），改 retrieval.FuseRRF（预建实例与每次新建行为等价，因 Fuse 无状态）

## 验证
- gofmt 干净；go build ./retrieval/... ./knowledge/... 通过
- retrieval + knowledge 全部测试通过（含 TestExtension_ThreeLaneRRF、TestBackendHook_RRFFusionBothChannels、hybrid_test.go）
- make verify 全绿（lint + check-arch + doc-check + build + test-race 四模块，62 秒）

## 影响
行为等价：融合算法与默认 k=60 完全不变；fileindex 结构体少一个字段；三处调用点统一到 retrieval 包单一实现。各处的多路 lists 收集逻辑（extension 并发三路 / writable 顺序两路 / fileindex 顺序四路）差异大且属领域逻辑，不强行合并。

---

## docs(agentcore): hook 双轨 Deprecated 注释如实化：承认兼容适配桥而非承诺移除

## 背景
中道审阅批 7（F2-3）：agentcore 的 hook 体系三层并存（ExecutionConfig.GlobalBefore/After → Middleware 适配、agentcore.LifecycleHook → observer 适配、iface 窄视图 → 原生接口适配），但两处 Deprecated 注释承诺了不会发生的移除（config.go "Will be removed in v0.6.0"、lifecycle.go "prefer implementing ...Observer"）。实际这两条路径是当前扩展机制（Extension.HookProvider → GlobalBefore/After、doomloop/memory/guardrails 等 17 个 LifecycleHook 实现者）的承重适配桥。

## 改动清单
1. agentcore/config.go：GlobalBefore/GlobalAfter 注释撤"Will be removed in v0.6.0"，如实标注"legacy hook slots，New() 自动适配为 Middleware；作为 HookProvider 与 Handoff 传播的适配路径，不计划移除"
2. agentcore/lifecycle.go：LifecycleHook 接口注释撤 Deprecated，改为"新代码优先用 observer 接口；LifecycleHook 作为粗粒度兼容面继续完全支持，经 BaseLifecycleHook 适配，不计划移除"

## 验证
- gofmt 干净；make verify 全绿（lint + check-arch + doc-check + build + test-race 四模块，67 秒）
- 行为零变化（纯注释修正，无逻辑改动）

## 影响
行为等价。注释不再误导未来维护者（此前暗示将移除实际活跃的适配路径）；新代码指引保留（优先 observer/Middleware）。批 7 其余三项（F3-1 adapter 确认不回退、F5-4 两引擎职责不同维持现状、F6-2 SkipDistortionDetection 有意预留维持现状）为决策结论，无代码改动，详见审阅计划文档。

---

## docs(domains): assemble.go 注释如实化：声明 legal Extensions 注册顺序漂移

injectDomainSharedExtensions 的注释原称"Tools 与 Extensions 的最终追加顺序不变"，经 code review 核对不实：Tools 段顺序一致，但 Extensions 段中 legal 的 Rules/Writing 相对原序交换（统一为 patent 序）。已实证两者无同名工具、无 LifecycleHook（仅 ToolProvider / SystemPromptProvider），注册顺序不影响行为；注释改为如实描述。验证：go build ./domains/... + go vet + domains 装配测试全绿。

---

## feat(tui/chat): 用户/智能体消息气泡背景色区分

背景：用户输入与智能体输出仅靠前缀/前景色区分，缺少背景色块，长会话中视觉区分度不足。

改动清单：
- semantic_theme.go：新增 AssistantMessageBg token；浅色 #f7f9fc（用户 #eceff1），深色 #0C1B2A（用户 #102638）
- palette.go：Palette 新增 UserBg/AssistantBg Style，BuildPalette 从 sem.UserMessageBg/AssistantMessageBg 构建（空值自动跳过，向后兼容）
- chat_history.go：ChatHistoryTheme 新增 UserBgStyle/AssistantBgStyle，DefaultChatHistoryTheme 绑定
- chat_history_render_message.go：新增 applyBubbleBg/applyBgOneLine/isSgrReset；用户消息（renderUserRole）与智能体消息（展开态+折叠态）整行背景填充；SGR 内嵌 Reset 后重新注入背景码防背景断裂；背景禁用时空行保持空串不膨胀；折叠态助手消息同款背景
- 参数名 theme ChatHistoryTheme → chatTheme，消除与 theme 包名遮蔽

验证：tui 模块 go build ./... 全绿；chat 包 go test -race 全过（含空助手消息用例适配背景填充）；root/tools/desktop 三模块构建通过。

影响：仅 tui 渲染层；未配置背景色（空 token）时行为与改动前一致。
