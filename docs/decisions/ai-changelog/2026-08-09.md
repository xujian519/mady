# 2026-08-09

## fix(tui): 全量审阅修复：P0 resize 防抖自续循环 + 12 项 P1 + 31 项 P2（TUI 2026-08-09 审阅）

**背景**：docs/review/tui-full-audit-2026-08-09.md 全量审阅（8 路走查）发现的缺陷批量修复。

**P0**：WindowSizeMsg 防抖自续循环（tui_input.go）——防抖回调回投原始消息导致 100ms 常驻循环、组件收不到 resize；改为内部 delayedWindowSizeMsg 包装解包分发，补回归测试。

**P1（12 项）**：VTE KKP 阈值 10 倍偏移（82000→8200）；Kitty PUA 0xe001-0xe003 映射错位；Raw→Cells 差分幽灵残留；resize 后光标永久隐藏；Editor 三处删除函数 merge 分支丢失 onChange；redo 键位注册 tui.editor.redo；footer/settings 渲染提前释放锁（深拷贝快照）；review_gate/command_center 手动解锁 panic 双重解锁（invokeLocked）；session_selector 回调 goroutine 改 Cmd 模式 + recover + 限时（M-1）；StartAutoThemeWatcher 仅 auto 主题生效；chat_history 工具组拼接丢成员；行首 **bold** 误判无序列表。

**P2（31 项）**：乘数器门提前（tmux 拦截优先）；iTerm2 入 KKP 正列表；Warp/Otty/GrokDesktop 入真彩白名单；CSI Z（Shift+Tab）解析；O_NONBLOCK Start/Stop 对称；未知绑定 ID 告警；WatchSystemAppearance Unknown 不误报；ToggleTheme 魔法 hex 改亮度阈值；Basic 模式数字索引折叠 16 色 + 极性动态推导；JSON 未知键坏类型不阻断加载；Raw 行差分补尾 reset；tab/控制字符丢弃；SGR colorspace 判定收紧；TruncateToWidth reset 判定；DiffCells raw 分支返回空 diff；嵌套列表标记剥离；行内数学星号保护；code span 哨兵防二次强调；表格吞并/超列/判定口径统一；TodoPanel Toggle 接线；skill_center 锁外回调；7 处宽度约束；置信度阈值统一（judgment_view 迁移共享渲染器）；QuitMsg 走 Filter；工具组渲染钳制；PlanTaskFeedback 映射测试补全。

**遗留**：P2-31 流式对账跨段需 agentcore 侧完整会话输出（跨模块，另行跟进）；死代码为公共导出 API 保守保留；doc-check 仓库级计数漂移非 TUI 模块。

**验证**：cd tui && go build/vet/test-race ./... 全绿（9 包）；golangci-lint 0 issues；verify_layers.sh 通过。TestAgentRunInTUISession 为依赖真实 LLM 的 flaky 集成测试（单独运行通过）。

---

## fix(tui): code-review 复核修复：Basic 模式调色板栈溢出回归 + 数字强调回归 + 锁外读竞争等 7 项

对 TUI 2026-08-09 审阅修复批做严格 code review（3 路并行审阅 + 主代理交叉验证），修复 7 项：1) P2-9 currentIsDarkBg 回读 CurrentPalette 与 BuildPalette 构成环依赖——ColorModeBasic（TERM=dumb/非 TTY）首次 palette 构建无限递归栈溢出；改为复用 SetSemanticTheme 维护的 isDark 状态，新增回归测试 TestCurrentIsDarkBgDoesNotBuildPalette。2) P2-17 guardMathAsterisks 单侧贴数字即守卫，破坏 **2**/*2* 数字强调；收紧为两侧贴数字，新增 TestRenderInlineMathGuardDoesNotBreakDigitEmphasis。3) P1-12 session_selector confirm/confirmDelete 锁外读 items[sel] 与 applyFilterLocked 原地复用竞争；改为锁内拷贝 item。4) session_selector setPending 双层 goroutine+recover+5s 超时为冗余机制（事件循环 execCmdIndexed 已有 panic-guard，超时不终止 goroutine）；简化为直通 Cmd。5) P2-16 stripNestedListMarker 仅套用 kindBullet，kindOrdered/kindChineseOrdered 续行仍泄漏嵌套标记；补齐。6) P1-4 auto watcher 不变量被绕过：ToggleTheme/JSON 热重载后 currentThemeName 仍为 auto，OS 外观变化会覆写手动选择；两处补 Store("")。7) gofmt 门禁 4 文件违规 + editor_edit.go redo 注释与 keybindings 事实相反；修复。验证：tui 模块 go build/vet/go test -race 全绿、gofmt clean、golangci-lint 0 issues、根模块 build OK。
