# 2026-08-12

## refactor(desktop): Sidebar 导航重构：Tab 竖排于搜索框上方，移除文件占位 Tab 与展开态图标

背景：左侧边栏顶部四个入口（会话/项目/文件/记忆）横排于 Header，用户提出按功能竖排并精简。改动清单：
1. Tab 切换按钮从 Header 横排移至独立竖向导航区（位于搜索框上方），会话/项目/记忆 按功能竖排；
2. 删除「文件」占位 Tab（SidebarTab 类型、tabs 数组、占位渲染块及 FileText 引用一并清理）；
3. 展开态 Tab 仅渲染文字标签（icon 退化为折叠态专用，代码注释说明）；
4. 互斥渲染合并为三元表达式，tabs 数组补 icon 语义注释。
验证：pnpm typecheck / pnpm lint / pnpm test:component（14 通过）/ pnpm build 全过。
影响：仅 desktop/frontend/src/components/Sidebar.tsx 单文件，无行为回归。

---

## feat(desktop): 知识库管理页新增嵌入/Rerank 模型设置（重启生效）

背景：嵌入/Rerank 模型此前仅由 OMLX_* 环境变量控制（bootstrap.BuildEmbedder/BuildReranker 启动时装配），桌面端无配置入口。\n\n改动清单：\n1. desktop/app_knowledge_settings.go（新）— Get/SetKnowledgeModelSettings + GetOmlxServiceStatus binding；持久化 ~/.mady/knowledge-settings.json（原子写，settingsMu 互斥）；API Key 仅返回掩码，Set 传掩码保持原值、传空串清空；applyKnowledgeModelEnv 在 initDeferred 调 LoadWikiStore 前注入 OMLX_* 环境变量（零改动 bootstrap）。\n2. desktop/app.go — initDeferred 增加启动注入调用。\n3. 前端 — backend.ts 封装 + queries/knowledge.ts（useQuery/useMutation）+ KnowledgeView.tsx 新增模型设置区（BaseURL/API Key/嵌入模型名/Rerank 开关与模型名/oMLX 服务状态）；wailsjs 声明同步。\n4. desktop/app_knowledge_settings_test.go（新）— 持久化 round-trip/掩码语义/校验/默认值合并/环境变量注入测试。\n\n验证：cd desktop && go build ./... && go test -race ./... 全过；golangci-lint 0 issues；前端 tsc + vite build + vitest 91 tests 全过。\n\n影响：切换嵌入模型不会重建预构建 knowledge.db 向量索引（BGE-M3 1024 维），维度不匹配时向量检索安全降级为 FTS-only（UI 已明示）；Rerank 无维度约束。保存后重启应用生效（与 Q9 AI 设置语义一致）。

---

## refactor(desktop): 代码审查重构：JSON 设置读写收敛为泛型 loadJSONFile/saveJSONFile

代码审查发现 saveAISettingsTo/loadAISettingsFrom 与知识库设置的新增 saveJSONFile/loadKnowledgeModelSettings 逻辑完全同构（重复的原子写/零值回退）。\n\n改动：\n1. app_settings.go 新增泛型 loadJSONFile[T any] / saveJSONFile（原子写 tmp+rename，G-I3），作为桌面端设置文件唯一读写入口；删除 saveAISettingsTo/loadAISettingsFrom。\n2. app_knowledge_settings.go 删除重复实现，改用 loadJSONFile[KnowledgeModelSettings]；移除不再使用的 encoding/json import。\n3. 调用点更新：app.go / app_settings.go / project.go / app_test.go / app_knowledge_settings_test.go。\n4. 前端 KnowledgeView.tsx 提取 inputCls 常量，消除三处重复的输入框类名。\n5. 补 TestGetOmlxServiceStatus_NoAPIKey（离线可跑，无网络依赖）。\n\n验证：cd desktop && go test -race ./... 全过；golangci-lint 0 issues；前端 tsc + vitest 91 tests + build 全过。行为不变。
