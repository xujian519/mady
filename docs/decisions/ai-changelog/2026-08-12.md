# 2026-08-12

## refactor(desktop): Sidebar 导航重构：Tab 竖排于搜索框上方，移除文件占位 Tab 与展开态图标

背景：左侧边栏顶部四个入口（会话/项目/文件/记忆）横排于 Header，用户提出按功能竖排并精简。改动清单：
1. Tab 切换按钮从 Header 横排移至独立竖向导航区（位于搜索框上方），会话/项目/记忆 按功能竖排；
2. 删除「文件」占位 Tab（SidebarTab 类型、tabs 数组、占位渲染块及 FileText 引用一并清理）；
3. 展开态 Tab 仅渲染文字标签（icon 退化为折叠态专用，代码注释说明）；
4. 互斥渲染合并为三元表达式，tabs 数组补 icon 语义注释。
验证：pnpm typecheck / pnpm lint / pnpm test:component（14 通过）/ pnpm build 全过。
影响：仅 desktop/frontend/src/components/Sidebar.tsx 单文件，无行为回归。

---

## feat(desktop): 知识库管理页新增嵌入/Rerank 模型设置（重启生效）

背景：嵌入/Rerank 模型此前仅由 OMLX_* 环境变量控制（bootstrap.BuildEmbedder/BuildReranker 启动时装配），桌面端无配置入口。\n\n改动清单：\n1. desktop/app_knowledge_settings.go（新）— Get/SetKnowledgeModelSettings + GetOmlxServiceStatus binding；持久化 ~/.mady/knowledge-settings.json（原子写，settingsMu 互斥）；API Key 仅返回掩码，Set 传掩码保持原值、传空串清空；applyKnowledgeModelEnv 在 initDeferred 调 LoadWikiStore 前注入 OMLX_* 环境变量（零改动 bootstrap）。\n2. desktop/app.go — initDeferred 增加启动注入调用。\n3. 前端 — backend.ts 封装 + queries/knowledge.ts（useQuery/useMutation）+ KnowledgeView.tsx 新增模型设置区（BaseURL/API Key/嵌入模型名/Rerank 开关与模型名/oMLX 服务状态）；wailsjs 声明同步。\n4. desktop/app_knowledge_settings_test.go（新）— 持久化 round-trip/掩码语义/校验/默认值合并/环境变量注入测试。\n\n验证：cd desktop && go build ./... && go test -race ./... 全过；golangci-lint 0 issues；前端 tsc + vite build + vitest 91 tests 全过。\n\n影响：切换嵌入模型不会重建预构建 knowledge.db 向量索引（BGE-M3 1024 维），维度不匹配时向量检索安全降级为 FTS-only（UI 已明示）；Rerank 无维度约束。保存后重启应用生效（与 Q9 AI 设置语义一致）。

---

## refactor(desktop): 代码审查重构：JSON 设置读写收敛为泛型 loadJSONFile/saveJSONFile

代码审查发现 saveAISettingsTo/loadAISettingsFrom 与知识库设置的新增 saveJSONFile/loadKnowledgeModelSettings 逻辑完全同构（重复的原子写/零值回退）。\n\n改动：\n1. app_settings.go 新增泛型 loadJSONFile[T any] / saveJSONFile（原子写 tmp+rename，G-I3），作为桌面端设置文件唯一读写入口；删除 saveAISettingsTo/loadAISettingsFrom。\n2. app_knowledge_settings.go 删除重复实现，改用 loadJSONFile[KnowledgeModelSettings]；移除不再使用的 encoding/json import。\n3. 调用点更新：app.go / app_settings.go / project.go / app_test.go / app_knowledge_settings_test.go。\n4. 前端 KnowledgeView.tsx 提取 inputCls 常量，消除三处重复的输入框类名。\n5. 补 TestGetOmlxServiceStatus_NoAPIKey（离线可跑，无网络依赖）。\n\n验证：cd desktop && go test -race ./... 全过；golangci-lint 0 issues；前端 tsc + vitest 91 tests + build 全过。行为不变。

---

## refactor(repo): 技术债务清理：version 子命令 / noctx 修复 / analysiskit+rulekit 测试 / find 测试稳定性

背景：2026-07-30 审计（78/100 B+）后的本周+短期债务项执行。改动清单：(1) cmd/mady 新增 version 子命令（-v/--version）输出版本信息，移除 var _, _ = commitHash, buildTime 死变量异味；(2) tools/browser_camofox.go 两处 HTTP 调用改用 http.NewRequestWithContext，消除 noctx 压制；(3) 移除 tools/desktop/computer_use_*.go 5 处文件级多余 noctx 压制（文件内无 HTTP 调用，行内 gosec,noctx 压制保留）；(4) domains/analysiskit 新增 analysis_test.go（97% 覆盖）、domains/rulekit 新增 rules_test.go（100% 覆盖）；(5) tools/find.go 新增 FdLookup 注入点修复 TestFindToolBasic 环境依赖 flaky（fd 存在时走真实搜索，结果依赖搜索目录内容），测试禁用 fd 路径保证确定性。验证：四模块 build/vet/golangci-lint 0 issues；tools go test -race 全过；analysiskit/rulekit/cmd/mady 测试通过；gofmt 干净。

---

## refactor(repo): 大文件合理拆分：8 个 >500 行文件按职责拆分

背景：全库 96 个 >500 行 Go 文件，本轮按'职责混杂优先、纯搬移不改逻辑'原则选 8 个拆分（不追求机械压到 500 行以内）。改动清单：bootstrap/setup.go(868)→setup_steps/setup_config/setup_assembly（初始化步骤/配置解析/装配辅助）；mcp/discovery.go(752)→discovery_types/discovery_client（类型定义/Client RPC）；domains/evidence/engine.go(626)→public_use.go（公开使用判断）；knowledge/extension.go(698)→extension_search/extension_tools（检索实现/工具 handler）；session/session_store.go(694)→session_store_migrate.go（版本迁移）；domains/infringement/nodes.go(708)→nodes_schema.go（JSON Schema）；memory/store.go(630)→scoring.go（共享评分函数，同时消除 store/sqlite_store 耦合）；agentcore/agent.go(566)→a2ui_promise.go（A2UIPromise 并发原语）。验证：四模块 build/vet 全过，根模块 go test ./... 全过，golangci-lint 0 issues。

---

## refactor(repo): 大文件合理拆分（第二轮）：8 个 >500 行文件按职责拆分

沿用首轮'职责混杂优先、纯搬移不改逻辑'标准，本轮拆分：provider/chatcompat/chat.go(809)→chat_types/chat_convert（wire 类型/消息转换）；agentcore/tool_gen.go(512)→tool_gen_schema/tool_gen_coerce（reflect schema 生成/参数类型 coerce）；memory/sqlite_store.go(660)→sqlite_store_query.go（SQL 查询辅助）；knowledge/fileindex/store.go(688)→store_scoring/store_utils（BM25 风格评分/文件分类与预览）；a2a/client.go(691)→client_http/task_stream（HTTP 重试基础设施/SSE TaskStream）；domains/search/commander.go(643)→commander_types/commander_utils（类型与策略表/结果提取工具）；domains/claimdrafting/builder.go(667)→builder_utils.go（包级工具函数）；disclosure/novelty.go(728)→novelty_prompts/novelty_utils（兜底 prompt 常量/输入构建与输出解析）。验证：根模块 build/vet/test 全过、三子模块 build 全过、golangci-lint 0 issues。

---

## refactor(repo): 大文件合理拆分（第三轮）：8 个 >500 行文件按职责拆分

沿用前两轮标准，本轮拆分：doomloop/doomloop.go(620)→detectors.go（6 个探测器与核心 DoomLoop/hook 分离）；agui/converter.go(723)→converter_convert/converter_helpers（convert* 私有方法/包级消息转换）；agentcore/compaction.go(646)→compaction_budget.go（预算/边界工具与 runCompaction 主体分离）；tui/terminal/detect.go(883)→detect_types/detect_env/detect_caps（品牌与多路复用枚举/环境变量检测/能力计算）；tui/overlay.go(776)→overlay_render.go（帧合成与渲染算法）；domains/workflows/patent/reexamination.go(813)→reexamination_prepare.go（4 个复审请求文案生成函数）；domains/workflows/patent/invalidation.go(796)→invalidation_parse.go（权利要求解析与无效理由识别）；agentcore/event_types.go(604)→event_serialize.go（事件 JSON 序列化与错误重建）。验证：根模块 build/vet/test 全过、三子模块 build 全过、golangci-lint 0 issues。

---

## refactor(repo): 大文件合理拆分（第四轮）：9 个 >500 行文件按职责拆分

沿用前几轮标准，本轮拆分：domains/workflows/patent/rule_engine.go(664)→rule_engine_checks.go（evaluateRule 与 20 个 check* 类型检查器）；guardrails/constitutional.go(579)→constitutional_rules.go（8 个宪法规则实现）；tui/core/width.go(580)→width_wrap.go（ANSI 换行算法）；tui/terminal/keys.go(649)→key_decode.go（CSI/SS3/Kitty 键盘序列解码）；a2a/ws.go(566)→ws_handlers.go（8 个 WebSocket handler）；a2a/server_jsonrpc.go(531)→jsonrpc_handlers.go（7 个 JSON-RPC handler）；tui/chat/chat_history.go(932)→chat_history_types.go（数据结构）+chat_search.go（搜索逻辑，170 行）；domains/workflows/patent/invalidation.go(700)→invalidation_nodes.go（10 个 Pregel 节点）。验证：根模块 build/vet/test 全过、三子模块 build 全过、golangci-lint 0 issues。

---

## refactor(repo): 大文件合理拆分（第五轮）：8 个 >500 行文件按职责拆分

沿用前几轮标准，本轮拆分：tui/chat/chat_app_layout.go(1040)→chat_app_frame.go（editorFrame/todoBar 等子组件）+chat_app_keys.go（键盘分发 dispatchKey 系列）；tui/chat/chat_history_render.go(734)→chat_render_tools.go（工具组渲染 renderToolGroup 系列）；tui/chat/chat_history.go(611)→chat_history_messages.go（Append/Delta/Finalize 等消息操作）；domains/workflows/patent/analysis.go(539)→analysis_nodes.go（新颖性图 6 个 Pregel 节点）；session/session_store.go(613)→session_store_filestore.go（FileStore 全部实现）；acp/server_handlers.go(578)→server_session_handlers.go（5 个会话 handler）+server_prompt_handlers.go（Prompt/模式/模型 handler 与提示提取）；evaluate/metrics.go(552)→metrics_citation.go（引用类指标）+metrics_misc.go（长度/证据指标）；domains/infringement/nodes.go(561)→nodes_utils.go（输入序列化与文本工具）。验证：根模块 build/vet/test 全过、三子模块 build 全过、golangci-lint 0 issues。

---

## fix(repo): 代码审查修复：注释拼接残留清理 + find 测试确定性

code-reviewer 审查全部未提交改动后修复 8 项：Important 3 项——(1) tools/tools_ops_test.go 的 TestFindToolConfigDefaults 移除对真实 fd 二进制的解析断言（恢复无环境依赖，已在 PATH=/nonexistent 下验证通过）；(2) tui/overlay.go 与 overlay_render.go 的 composeOverlays 文档注释被切分残留两半，已合并完整注释并删除孤儿行；(3) bootstrap/setup_assembly.go 删除 NewInfringementKnowledgeRetriever 孤儿注释副本，bootstrap/infringement_adapter.go 恢复缺失的末行。Minor 5 项——setup_steps.go 重复注释行、chat_history.go 的 ChatRole 注释错位（移回 chat_history_types.go）、chat_history.go 文件头所有权映射更新、setup_assembly.go 段标题改为'装配辅助函数'、metrics_citation.go 残留分隔行。验证：根模块 build/vet/test 全过、三子模块 build 全过、golangci-lint 0 issues。
