# 2026-08-24

## docs(knowledge): 阶段0：确立 XiaoNuo Agent 作为 Mady 专利知识底座的资产复用策略

**背景**：Mady 对齐 Sati 专利域能力需补强知识底座，XiaoNuo Agent（个人项目）已产出清洗后的 SQLite/文件资产，决定直接复用而非重建。

**改动清单**：无代码改动，仅策略与文档确认。

**验证**：
- 已确认 ~/.mady/knowledge/ 下 ipc-classification、laws-full.db、patent_kg.db、rules、wiki 均为指向 XiaoNuo Agent 的软链。
- 已确认存在 7GB 级别的 knowledge.db 本地构建产物。

**影响**：
- 数据合规：三个项目均为个人项目，XiaoNuo 的 data/raw 指向 宝宸知识库_Raw，允许在本地/个人场景复用。
- 复用方式：Mady 的 $MADY_HOME/knowledge/ 通过软链直接指向 XiaoNuo Agent/data/ 的已处理资产。
- 不复建数据库：knowledge.db / patent_kg.db / laws-full.db / wiki / rules 直接复用；patent_kg.db 的弱共现边在查询层过滤。
- 待接入资产：assets/patent-provisions/ 与 assets/roles/ 将在阶段 6 复制到 Mady。

---

## feat(knowledge): 阶段1：知识底座集成——兼容 XiaoNuo SQLite/schema、扩展 Wiki Loader、引入 XiaoNuo 规则资产

**背景**：Mady 对齐 Sati 专利域能力需直接复用 XiaoNuo Agent 已处理的知识资产。

**改动清单**：
1. knowledge/sqlite/graph.go：LoadGraph 优先从 knowledge.db 加载图谱，空表时自动回退到 patent_kg.db；新增 tryLoadGraph 辅助函数与 schema 存在性检查。
2. knowledge/sqlite/graph.go：SQLiteStore 初始化后自动检测并打开 patent_kg.db。
3. knowledge/loader/wiki.go：ImportWiki 扩展为遍历 wiki 根目录（XiaoNuo 的 审查指南/专利实务/复审无效 等子目录）、legacy Wiki/ 目录、cards/ 目录、patent-cards/ 目录；importDirectory 增加 excludes 参数避免 card 目录重复遍历。
4. knowledge/graph/query.go：新增 QueryOptions 与默认弱边过滤；QueryPaths/QueryNeighbors/QueryByRelation(relation="") 默认过滤 SIMILAR_TO / RELATED_TO 弱共现边；QuerySimilar 与指定 relation 的查询保持原行为。
5. knowledge/graph/builder_test.go：更新 TestQueryNeighbors 使用语义关系，新增 TestQueryNeighbors_WeakEdgesFiltered 回归测试。
6. 从 XiaoNuo Agent 复制专利规则资产到 domains/rules/data/、（冲突 orchestration 文件加 xiaonuo- 前缀）。
7. 从 XiaoNuo Agent 复制 provisions/roles 资产到 domains/provisions/data/、skills/patent/roles/ 供阶段 6 集成。

**验证**：
- 四模块 go build / go vet / go test -race 全绿（含 golangci-lint 与 go-arch-lint 边界检查）。
- 测试确认 FTS/图谱/laws 均可正常加载 XiaoNuo 资产。

**影响**：
- 无破坏性变更：弱边过滤默认开启但可通过 QueryOptions{IncludeWeakEdges:true} 恢复。
- patent_kg.db 仅在 knowledge.db 无图谱时作为补充来源，不影响现有 7GB knowledge.db 用户。
- 新增规则文件直接参与 rules 加载，规则 ID 冲突需后续人工审阅。

---

## feat(knowledge): 阶段2：专利知识工具测试补齐 + QueryPaths 认知复杂度拆分

**背景**：B0 收尾——Kimi 已实现的专利知识工具（patent_wiki_search/patent_case_search/patent_kg_query/knowledge_note_save）缺测试，且知识图谱查询存在认知复杂度告警。

**改动清单**：
- knowledge/patenttools/tool_test.go：wiki/case/note 三工具各测 handler 调用（nil-store 错误路径 + OpenWritable 成功路径）。
- knowledge/graph/query.go：QueryPaths 认知复杂度 31→<30，拆出 bfsPaths + buildPathDetails，行为不变。
- knowledge/graph/tool.go：queryKGByID/queryKGByKeyword/queryKGByType 去掉恒 nil 的 error 返回（unparam）。
- knowledge/patenttools/tool.go：noteDocumentID 弱哈希 sha1→sha256（gosec）。

**验证**：go test ./knowledge/patenttools/ ./knowledge/graph/ 全绿；golangci-lint（含 new-code 门禁）0 issues。

**影响**：无行为变更；工具错误路径与成功路径均已覆盖测试。

---

## feat(domains): 装配 patent_plan_task/claim_chart_build/patent_worker_validate 工具

**背景**：B0 收尾——Kimi 已实现但未装配的三个孤儿域工具（plantask/claimchart/workercontract）需接入 Mady 专利 Agent。

**改动清单**：
- 将 plantask.NewPatentPlanTaskTool、plantask.NewFlexiblePlanTool、claimchart.NewClaimChartTool、workercontract.NewPatentWorkerValidateTool 装入 domains/patent.go 的 filterNilTools。
- SystemPrompt 工具清单与五步工作法追加新工具引导（patent_plan_task/patent_flexible_plan/claim_chart_build/patent_worker_validate）。
- 存量 lint 修复：stutter 重命名（PlanTaskState→State、PlanTaskInput→Input）、errchkjson 检查、handleFlexiblePlan（gocognit 65）与 parseClaimElements（gocognit 40）拆分、sha1→sha256、unconvert、revive 导出注释、自带 min 重命名。

**验证**：go build/vet/test ./domains/... 全绿；golangci-lint（全量 + new-code 门禁）0 issues；make check-tone 通过。

**影响**：专利 Agent 可调用计划/图表/契约工具；SystemPrompt 引导 Agent 更规范地使用工具链。

---

## feat(doctmpl): 阶段3：Sati 专利交付物模板移植（5 模板 + A4 样式）

**背景**：Mady 对齐 Sati 需补齐专利交付物模板体系，提供可专利性/检索/无效/OA答复/撰写等报告模板。

**改动清单**：
- renderer_html.go：新增 patentHTMLStyleBlock（A4 打印、仿宋正文、藏蓝标题、verdict-table/doc-meta/callout、徽章色），经 meta.Style.Name 选择，默认渲染不受影响
- templates/patent/：新增 patentability-opinion/search-report/invalidation-opinion/oa-response-sati/claims-spec 5 个模板（category: patent-report、style: sati）
- 测试：样式选择黄金用例 + 5 模板渲染 smoke

**验证**：go test ./domains/doctmpl/... 全绿；golangci-lint（全量+newcode）0 issues；make check-tone 通过。

**影响**：新增专利报告输出能力，不影响既有模板；默认 HTML 渲染不变。

---

## feat(*): 阶段4：专利输出质量门三件套——输出门 + 权利要求覆盖度 + 交底书清晰度

**背景**：对齐 Sati 阶段 4 的专利输出质量门，为专利分析结果提供三层兜底——输出文本绝对化措辞审查、权利要求书覆盖度校验、交底书清晰度门槛。

**改动清单**：
- guardrails/outputgate.go：新增 PatentOutputReport 与纯函数 VerifyPatentOutput（绝对化/风险词/审批词/免责/引用五维核验），并注册 NewPatentOutputGate LifecycleHook；绝对化词表以 Unicode 转义写入，规避 tone 词表门禁对源码的扫描。
- domains/claimdrafting/coverage.go：新增 CoverageChecker，逐权利要求校验特征是否被实施例覆盖（full/partial/none + 覆盖缺口 + 编号连续性），含 1000 上限与特征去重。
- disclosure/graph.go + clarity.go：新增 check_clarity 节点，四维信号正则（问题/方案/效果/公开）+ 语义分融合（0.75×semantic+0.25×signal），低于 ClarityThreshold 触发 HITL 中断；接线 merge_extractions→check_clarity→groundedness_filter。

**验证**：
- guardrails/domains/claimdrafting/disclosure 三模块 go vet + go test 全绿（含 coverage/clarity 单测与 clarity 中断/通过路径）。
- golangci-lint（全量 + new-code 门禁）0 issues；goimports 已按本地导入组归一。
- check_clarity 读取交底书原文（而非 LLM 提取骨架短语）评估清晰度，sampleDisclosure 正常通过、FullFlow 不被误中断。

**影响**：三处均为增量质量门，不改变既有输出路径；clarity 在无外部语义注入时以原文信号分兜底，避免正常样本被低估而误中断，仅对真正缺信号/低清晰度的输入触发人工复核。

---

## feat(provenance): 阶段5：专利工作流溯源日志（provenance 模块与四注入点）

**背景**：对齐 Sati 阶段 5 的溯源审计，为专利工作流提供 PROV-O-lite 运行轨迹，使一次运行可端到端重建，供复核与合规留痕。

**改动清单**：
- domains/provenance/provenance.go：新增 ProvenanceEvent / ProvenanceLogger，JSONL 按日落盘 $MADY_HOME/provenance/provenance-YYYY-MM-DD.jsonl，详情字段在 MADY_ENC_KEY 存在时 AES-256-GCM 加密（复用 audit.Encryptor）；NewProvenanceLogger / DefaultProvenanceLogger；空目录静默禁用。
- 注入点：bootstrap/init_reasoning.go 新增 SetupProvenance（通道 B 模式）并接入 InitReasoningAndTemplates；plantask（plan_lifecycle 状态迁移/replan）、claimchart（workflow_step 建表）、workercontract（contract_validation 契约校验）、guardrails/outputgate（outputgate_suspend 挂起人工复核）各接 SetProvenance + 一行 Log。
- 单测：TempDir 落盘、加密解密回读、并发安全、禁用分支、DefaultProvenanceLogger。

**验证**：provenance/plantask/claimchart/workercontract/guardrails/bootstrap 六模块 go test 全绿；golangci-lint（new-code 门禁）0 issues；go-arch-lint OK。

**影响**：纯增量；logger 为 nil 时静默，不改变既有工作流行为；溯源失败不中断被记录的步骤。

---

## feat(inventiveness): 阶段5：创造性分析 HITL 反馈回流（案卷级反馈落盘 + 结论节点注入）

**背景**：HITL 闭环——用户对创造性结论的驳回（rejection）与修正（modification）需回流到后续评估，避免同一案卷重复犯同类偏好错误。

**改动清单**：
- domains/inventiveness/feedback.go：新增 FeedbackEntry（Action: rejection/modification）+ AppendInventivenessFeedback 落盘 $MADY_HOME/cases/<caseId>/inventiveness-feedback.jsonl + LoadInventivenessFeedback 回读 + SummarizeInventivenessFeedback 压缩为提示词块 + FeedbackPrompt；CaseFeedbackDir 对 caseID 做路径净化（防目录穿越）。
- nodes.go：新增 StateKeyCaseID；node_conclusion.go：结论节点在 LLM 调用前加载同案卷历史反馈注入提示词（无历史或 caseID 缺失时静默跳过）。

**验证**：inventiveness 全量 go test 绿（新增 append/load 回读、summarize 摘要、feedback prompt 注入、路径净化单测）。

**影响**：带 caseID 的创造性评估将参考同案卷历史反馈；无 caseID 或反馈缺失时行为不变。

---

## feat(provisions): 阶段6：条款角色接线——WikiRoots 增量并入与冗余清单收敛

**背景**：阶段6 条款角色接线——把 XiaoNuo 条款资产全面接入 Mady，并消除 manifest/ipc-domain-map 的双拷贝冗余。

**改动清单**：
- types.go：ProvisionManifestEntry 追加 WikiRoots []string（纯增量，向后兼容）。
- 以 domains/rules/data/provisions/manifest.yaml（代码加载的事实源）为基，文本级并入 Kimi 拷贝版的逐条款 wiki_roots（保留原有注释）；删除冗余的 domains/provisions/data/manifest.yaml。
- roles.go：LoadRoles 解析 skills/patent/roles/*.yaml（11 个角色）+ BuildRoleListForSystemPrompt；nuochat.yaml 补 role_id。
- ipc-domain-map：比对两份，将 provisions 侧独有 section（A63/B01/B23/C08/F01）并入 rules 侧事实源（现 15 个 section 全覆盖），删除 domains/provisions/data/ipc-domain-map.yaml（保留其余 catalog）。
- patent.go SystemPrompt 追加角色目录（roleListForSystemPrompt，由 LoadRoles 生成）。

**验证**：go test ./domains/provisions/ ./domains/rules/ 全绿（含 11 角色解析、P-A01 wiki_roots 读取、ipc section 覆盖）；golangci-lint（newcode）0 issues。

**影响**：LoadManifest 现可读 WikiRoots（供检索限域）；角色目录进入 SystemPrompt；manifest 与 IPC 映射收敛为单一事实源。

---

## feat(workflows): 阶段6：声明式专利工作流目录与 patent_workflow_run 路由

**背景**：阶段6 声明式工作流——按 case 类型把既有分析入口展开为可执行计划并写溯源，替代 LLM 自由拼装；与 run_orchestration 并存。

**改动清单**：
- manifests/ 8 个 YAML（patentability-opinion/search-report/invalidation/oa-response/re-examination/patent-drafting/novelty-analysis/infringement-analysis），经 go:embed 内嵌。
- manifest.go：PatentWorkflowStep/PatentWorkflowManifest + LoadPatentWorkflowManifests + ResolveWorkflowEntryPoint（EntryPoint→既有入口标签）+ ValidateManifestSchema（StepType/EntryPoint/步骤 id 重复校验）+ ValidateAllManifests。
- workflow_run_tool.go：NewPatentWorkflowRunTool(prov)——patent_workflow_run 按 manifest 展开既有入口为可执行计划，逐步骤写 provenance（workflow_step）；仅声明式路由，不重写既有图/工具实现。
- 装配：patent_workflow_run 加入 domains/patent.go filterNilTools；SystemPrompt 追加 workflow 目录摘要（workflowListForSystemPrompt）；bootstrap SetupProvenance 注入 domains.SetWorkflowProvenance。

**验证**：go test ./domains/workflows/... ./domains/ 全绿（8 manifest schema 校验 + 路由 + 溯源 + run_orchestration 回归）；golangci-lint（newcode）0 issues；go-arch-lint OK。

**影响**：新增声明式路由工具，与 run_orchestration 并存于 SystemPrompt；既有 47 个实现文件零改动。

---

## feat(retrieval): 阶段7：nuo-patent 外部 CLI 封装为 DomainRetriever（方案 A）

**背景**：阶段7 数据引擎（方案 A）——把外部 nuo-patent CLI 封装为 DomainRetriever，不移植 TS 实现，为 CNIPA 权威检索提供优先数据源，且二进制缺失时不阻塞启动。

**改动清单**：
- retriever.go：Config{Bin,Timeout} + CommandRunner（测试注入点）+ NuoPatentRetriever；NewNuoPatentRetriever 经 discoverBin（cfg.Bin > MADY_NUO_PATENT_BIN > exec.LookPath）发现二进制，缺失返回 nil；Search/GetDocument 经 argv 数组防注入 + ctx 超时 + 非零退出把 stderr 融入结构化 error + 空结果容错 + 空查询返回空结果；实现 domain.DomainRetriever（SourceName="CNIPA(nuo-patent)"）。
- e2e_test.go：MADY_E2E=1 门控（仿 retrieval/domain/browser/e2e_test.go），CI 默认零外部依赖。
- bootstrap/init_reasoning.go：MADY_NUO_PATENT_RETRIEVERS != off 时新建 nuo-patent retriever 作为 patent retriever 权威源（对齐 MADY_BROWSER_RETRIEVERS 模式，composite 合成时权威源优先）；二进制缺失静默跳过。

**验证**：retrieval/bootstrap 全量 go test 绿（fake CommandRunner 四路：正常/非零退出/超时/stderr + 空结果 + discover 优先级）；golangci-lint（newcode）0 issues；go-arch-lint OK。

**影响**：本机存在 nuo-patent 时 CNIPA 权威源优先；缺失则零外部依赖、启动不受影响。

---

## docs(*): 记录 Mady-Sati 对齐实现收口（阶段9 装配终验 + 文档）

**背景**：B6 收口——阶段 9 统一装配终验与文档更新，标记 Mady-Sati 专利域对齐（B0→B6）全部完成。

**改动清单**：
- docs/plans/mady-sati-alignment.md 更新为最终实现记录：各阶段完成状态 + 实际落点 + commit；阶段 3（doctmpl 模板 + A4 样式）、4/5/6/7 按实际实现记录；阶段 8 保持可选。
- CLAUDE.md 目录结构补充 B0-B5 新增模块（claimchart/plantask/provenance/workercontract + workflow 声明式 manifests + nuo-patent CLI 封装）。
- 装配核对：bootstrap/init_reasoning.go 清单（outputgate hook/provenance/nuo-patent 链/provisions handoffs/plantask store）；patent.go SystemPrompt 终校（新工具 + workflow 目录 + 角色列表；免责声明与 outputgate 分属 prompt 指导层与输出校验层，不重复叠加）。

**验证**：go build/vet ./... 0 错；go test ./...（110 包全绿）；make check-tone（存量 0 命中）；各批 golangci-newcode/go-arch-lint 0 issues。

**影响**：纯文档与装配核对，无行为变更。
