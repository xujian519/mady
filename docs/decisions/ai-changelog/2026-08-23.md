# 2026-08-23

## refactor(devtools): 开发规范优化：三模块收敛、负控制自证与 fail-closed 硬化

**背景**：参照最小门禁集模板与四层规范体系审计，发现三类系统性问题——tools 并入根模块后 go.work/文档/CI 仍按四模块维护（高危漂移）、AI_CHANGELOG 迁移后 pre-commit 门禁指向旧文件从未触发（死门禁）、门禁无负控制自证且降级路径过多（跑过 verify≠通过门禁）。

**改动清单**：
- P0 三模块收敛：删 tools/.golangci.yml 与全部 cd tools 操作；AI_CHANGELOG 门禁接线（files 指向日期文件）；GO-STANDARDS 虚假承诺修复（启用 revive dot-imports）
- P1 负控制与 fail-closed：lint/doc-check/precommit 工具缺失 exit 1；覆盖率本地阈值（check-coverage.py 单源解析 codecov.yml，三模块加权 61.04% ≥ 53%）；newcode 生成式同步；golangci 版本单源化（.golangci-version）；doc-check 断言 5-13
- P2 元规则：AGENTS.md 规范元规则六条、GO-STANDARDS §0.4 诚实原则、docs/adr 与 docs/decisions README、tone 禁用词扫描（先评估后决策，未上门禁）
- 负控制自证 7 项全过（故意违规→看红→回退，含门禁自测捕捉门禁被削弱）

**验证**：make verify 全绿；go test ./scripts/gatechecks 红绿 7 测试通过；doc-check 断言 5-13 全绿；golangci-lint 0 issues。

**影响**：门禁体系从自觉维护转为机器自证；覆盖率定位为死代码探测器不进 verify 链；tone 词表上门禁与否待人工决策。

---

## feat(ci): tone 词表门禁上线（精化词表 + 限定目录扫描 + 负控制自测）

**背景**：AGENTS.md 安全红线「护栏文案、报告结论措辞」此前仅靠人工对照 docs/tone-style-guide.md 禁用词表，无机器验证。探针实测全量扫描 591 行命中，其中「侵权」82 文件命中绝大多数是业务术语/法条用语误报（domains/infringement 领域名、26.4「不支持」等），裸词词表不可直接上门禁。
**改动清单**：① §4 词表精化为断言式短语（存在/构成/属于/判定/确认 + 侵权/冲突），按严重度分三类（绝对化词/生硬拒绝/未经确认的断言）；② check-tone-words.py 限定面向用户文案目录（guardrails/server/agui/a2ui/pkg/i18n）、跳过代码注释与测试数据、EXEMPT 显式豁免 3 个门禁自身定义文件（各带理由）；词表仍单源解析 §4 表格；--fail 命中即 exit 1；③ pre-commit 新增 check-tone-words hook；④ Makefile check-tone target（fail-closed）；⑤ gate_test.go 新增 TestToneWordsGate 红/绿自测。
**验证**：存量 0 命中（限定范围后）；脚本直跑 + pre-commit run 双链路自证红绿（含注释中违规词被正确跳过）；门禁自测 6 组全过；doc-check 通过；提交时全部 14 个 hooks Passed。
**影响**：面向用户文案目录的 .go 变更新增一道措辞门禁；新增禁用词需同步改 §4 表格（单源）；业务术语目录（domains/ 等）不受影响。
