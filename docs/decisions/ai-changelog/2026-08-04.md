# 2026-08-04

## refactor(tui): TUI 全量审阅修复：14 项 P0/P1 缺陷 + FSM 全面接管 + 死代码清理 + 文档同步

对 TUI 模块（138 次提交反复调整）进行全量审阅（5 个并行审阅 + 关键缺陷人工复现），并执行 7 个阶段修复：

**P0/P1 正确性（A 工作流，均有回归测试且验证可捕获旧 bug）**
- pendingMotion 鼠标节流竞态（tui_input/tui_loop 加锁，-race 压力测试）
- WrapAnsi 宽度 1 + CJK 宽字符无限循环挂死（findBreakColumn 返回整字符宽度 + 迭代上限）
- Kitty 键盘协议功能键码位整体错位（57358 等 PUA 码位，对照 ultraviolet/官方表修正 + 表驱动测试）
- 浅色主题缺 Phase-1 token（Background/Surface 等回退到深色品牌值）
- overlay 鼠标坐标翻译失效（composeOverlays 补写 renderedWidth/Height）
- Editor 换行 chip 双重递增、横向表格表头裸 ** 泄漏、Table.VisibleRange RLock 写、Footer 空组 panic

**FSM 全面接管（F 工作流，替代渐进式中间态）**
- 审批退出卡死（AwaitingConfirm 缺 evtAgentStart 分支）
- ConfirmPending 接线到 Transition；确认提示从任意状态可达
- 删除 Running/StreamID/ActiveTools 兼容 shim 与 updateJudgmentView 回退启发式，FSM 成为唯一状态真相
- 清理 evtAutoRetry 死事件

**chat 正确性**
- 折叠 thinking 下标错位（msgRange 索引 vs messages 索引）
- 逐字符流 dedup 损坏（单 rune 放行，|---|---| 不再缩成 |-|）
- Render 早返回路径宽度钳制

**死代码清理（C 工作流）**：FirstRunWizard/OSC52/RegisterFocusCycle 家族/QuantizeTheme no-op 家族/终端接口 7 死方法/ansi 9 死函数/死字段等 ~25 符号（保留 SetThemeByName 等有生产调用者者）

**SGR 双模型（S 工作流）**：跨模型往返测试锁定 theme↔core 桥接不变量 + ADR 记录双模型现状

**测试基建（E 工作流）**：3 个未跟踪 repro 测试内嵌 fixture 入库（去桌面绝对路径/t.Logf，补中间帧宽度断言）；8 处固定 sleep 改轮询（新增 waitTUIStarted 探针）

**文档同步（D 工作流）**：LAYERS.md 幻影引用（CellGrid/BuildSGR/旧错误模型/AgentRunner）、ADR stdio 描述、doc.go 示例、MADY_KITTY_FLAGS env 实现化等

验证：tui 模块 build/vet/lint(0 issues)/test-race 全绿；根目录 make verify 四模块全绿。

---

## refactor(tui/chat): Code-review 修复：FSM 全局转移 hoist + Kitty C0 码位补全 + confirm 三函数去重

按严格 code-review 结论对上一轮 TUI 审阅修复做结构性收紧（行为零变化）：

**P1：FSM 全局转移 hoist**
- state.go：evtConfirmRequest 从 8 个 transitionFrom* 中提升到 Transition() 入口统一处理（），删除 8 处重复分支；同步补全 lattice 文档（新增 ConfirmPending/ConfirmRequest/MessageDelta/AwaitingConfirm-AgentStart 转移，删除未实现的 Failed--Interrupt 行）

**P2：Kitty C0 PUA 码位补全**
- keys.go：补 0xe000-0xe003（Escape/Tab/Backspace/Enter 的 PUA 编码，flag 1 下带修饰键上送），否则 Shift+Esc 等被当作私用区字符插入编辑器；全段改十六进制字面量 + TestKittyC0PUAKeys 测试

**P2：confirm 链路收紧**
- chat_app.go：ConfirmYes/ConfirmNo/confirmTimeout 提取公共 resolveConfirm（55→25 行）；StartConfirm 先 finalizeStreamLocked 再进 ConfirmPending（防止打断流时消息悬挂 Pending）；标注整条链路暂无生产调用方

**P2/P3：杂项**
- updateJudgmentView 补 StateConfirmPending 分支（status='confirming'）
- 删除 ansi_test.go 空壳 TestCursorMovement、quantize.go 悬空头注释、onboarding.go 空 section 头、测试名 typo
- onThrottledMotion 删除 send 布尔标志（提前 return）

验证：tui 模块 build/vet/lint(0)/test-race 全绿；make verify 四模块全绿。

---

## docs(desktop): 桌面端全面审阅对齐：Reasonix 参考计划状态更新与决策固化

**背景**：2026-08-04 对 desktop/（Go 后端 14 源 + 前端 src/ + 设计文档 11 份 + Reasonix 参考计划）
进行全面审阅，发现 4 个 P0 功能级 bug、16 个中危缺陷及一批技术债务；对照
`docs/plans/desktop-reasonix-reference-plan.md` 核对落地缺口，需要固化决策并同步文档状态。

**改动清单**：
1. `docs/plans/desktop-reasonix-reference-plan.md`：状态「方案评审中」→「已实施」；
   新增 G 节实施状态核对表（逐项 ✅/⚠️/❌）、F 节决策 5-7。
2. 决策 5（threadId 双真相源）：采用**标签联动**——侧栏点击会话=激活/新建绑定标签，
   新增 `BindThreadToSession` 绑定，消除「消息落 A 会话、UI 高亮 B 会话」撕裂。
3. 决策 6（wailsjs 提交策略）：提交当前差异，保持提交物=本地 `wails dev` 生成物。
4. 决策 7（会话重命名 UI）：`RenameThread` 绑定前端接入，补阶段 1.4 最后一块。

**设计决策**：标签联动（决策 5）与 Reasonix 参考计划阶段 4「完整多标签状态机」方向一致；
侧栏不再独立维护「当前会话」，聊天上下文唯一由标签驱动。

**影响**：文档与代码状态对齐；为批次 A/B 修复（另条记录）提供决策依据。

---

## fix(desktop): 桌面端审阅修复：TodoDock/记忆时间/窗口位置/MCP 脱敏等 P0-P1 问题

**背景**：2026-08-04 全面审阅发现 4 个 P0 功能级 bug（TodoDock 事件未订阅、MemoryEntry
字段名不匹配、窗口 X/Y 双写入者覆盖、MCP 脱敏空格分隔绕过）及 10 个中危正确性缺陷；
决策 5/7（标签联动、会话重命名）经用户确认后随批次 A/B 一并落地。

**改动清单**：
1. `agui-bridge/client.ts`：补订阅 task-created/task-updated，TodoDock 任务坞恢复。
2. `lib/backend.ts` + `MemoryPanel.tsx`：MemoryEntry 字段名对齐 snake_case，记忆时间恢复。
3. `window_state.go`：saveWindowState load-modify-merge，修复 X/Y 被 SaveWindowState 覆盖。
4. `app_mcp.go` + `app_mcp_test.go`：sanitizeMcpArgs 状态化脱敏（空格分隔形式）+ 单测。
5. `agui-bridge/reducer.ts`：agui:done 的 error 字段反馈 UI。
6. `ConfirmDialog.tsx`（新增）+ `ProjectTree.tsx`/`SkillsView.tsx`/`FileViewerOverlay.tsx`：
   window.confirm/alert 替换（WKWebView 不支持同步弹窗）。
7. `ChatView.tsx` + `MessageBubble.tsx`：历史消息 timestamp/role 映射 + formatTime 容错
   （消除 NaN:NaN）；round-header 外层 key 唯一化；专注模式 toggle 双向。
8. `Composer.tsx`：/theme light|dark|system 语义（明暗切换 vs 主题包切换分流）。
9. `app_files.go`：WriteFile 保留既有文件权限；`tabs.go`：tabStore.save() 原子写。
10. `tabs.go`/`tabs_app.go`/`tabs_test.go` + `Sidebar.tsx`/`stores/tabs.ts`：标签联动
    （BindThreadToSession + 侧栏激活/绑定）。
11. `ThreadItem.tsx` + `stores/threads.ts` + `Sidebar.tsx`：会话重命名行内编辑 UI。
12. wailsjs 经 `wails generate module` 重新生成（含新绑定），提交物与本地一致。

**设计决策**：标签联动采用「Go 侧真相源 + 前端缓存」；ConfirmDialog 复用 ModalShell 焦点
陷阱；WriteFile 权限策略为「既有文件沿用原权限、新文件 0600」。

**影响**：4 个 P0 bug 消除；TodoDock/记忆面板/窗口位置/会话重命名功能恢复可用；
threadId 双真相源撕裂消除。验证：go build/vet/test（含 -race）、tsc、vitest 86/86、
eslint 0 error 全过。

---

## refactor(desktop): 批次 C：测试假阳性修复、版本单一来源、工程卫生清理

**背景**：2026-08-04 全面审阅批次 C（P1-P2 工程卫生）：修复测试假阳性（断言与实现脱节、
e2e 伪检测），消除版本号三处重复维护，清理死代码/未用依赖并清零 14 个 eslint warnings。

**改动清单**：
1. `app_test.go`：原子写断言改为检查 `.mady-settings-*` 前缀（原断言目标文件模式错误，恒通过无意义）。
2. `e2e_integration_test.go`：正名为冒烟测试；删除恒 false 的 `isWailsContext` 伪检测；
   Chat 测试显式 Skip 并注明需实机验证；修正注释编号错乱。
3. `Makefile`：`DESKTOP_VERSION` 改为从 `desktop/wails.json` 读取（版本单一来源，
   消除 app_update.go / wails.json / Makefile 三处漂移）。
4. `app.go`：`initDeferred` panic 路径补 `SyncConfig` 收尾（避免阶段 2 扩展永久缺失）。
5. `main_unsupported.go`：双 `os.Exit(1)` 简化。
6. `menu.go`：菜单按 macOS 惯例重构——「Mady」应用菜单（关于版本对话框 / 设置 ⌘, / 退出 ⌘Q），
   删除「文件」菜单；保留 Edit/Window role 菜单。
7. 死代码清理：删除 `AgentFooter`/`ConclusionCard`/`ConfidenceBar` 组件 + barrel `index.ts`、
   `lib/utils.ts`（cn 无调用方）、`backend.deleteThread`（硬删无调用方）、
   `wails.emitWailsEvent`、settings store 死字段（themePack/agentPanelWidth/fileViewerOpen）、
   package.json 未用依赖（clsx/tailwind-merge/cva，vite manualChunks 同步）。
8. eslint 14 warnings 清零：6 个 a2ui 组件 missing dep 'context' 补全；
   `TextField` derived-state 改官方 render-setState 模式（去 ref）；`CodeEditor` latest-ref
   改 useEffect 同步；`renderer` store 依赖补全；`TemplatesView` useMemo 依赖稳定化；
   删除 2 个 unused eslint-disable。
9. `window_state.go`：slog 统一为 log（与其余桌面端文件一致）。
10. 删除 `desktop/renamed-folder/`、`desktop/test-folder/` 测试遗留空目录。

**设计决策**：版本号以 `wails.json` 为单一权威源，Makefile 读取注入 ldflags；
菜单布局遵循 macOS 惯例（首菜单为应用菜单）。

**影响**：测试从「恒通过/恒跳过」恢复真实断言；版本号不再三处漂移；
死代码与未用依赖移除（前端 bundle ui chunk 缩小）；lint 0 告警。
验证：go build/vet/test、tsc、vitest 86/86、eslint 0、vite build 全过。

---

## feat(desktop): 批次 D：Composer 粘贴图片 + CheckUpdate 中间态文案

**背景**：2026-08-04 审阅批次 D（Reasonix 参考计划收尾）：补齐 A.3「Composer 增强」唯一
未落地项（粘贴图片），并修正 CheckUpdate 占位实现的误导性文案。

**改动清单**：
1. `app_image.go`（新增）+ `app_image_test.go`（新增）：`SavePastedImage` 绑定——data URL
   解码 → 项目根 `attachments/` 保存 → 返回相对路径；白名单 MIME（png/jpeg/gif/webp）、
   10MB 上限、沙箱防御校验；`decodePastedImage` 纯函数单测 6 例。
2. `MarkdownRenderer.tsx`：支持 `![alt](src)` 图片渲染——http/data URL 直渲，
   项目内相对路径经 `ReadFile` 读 base64（ImageNode / ProjectImage 组件）。
3. `Composer.tsx`：粘贴图片——clipboardData 检测 image item → FileReader →
   `savePastedImage` → 光标处插入 Markdown 引用（insertAtCursor），附「正在保存」提示。
4. `app_update.go`：CheckUpdate 中间态文案改为「更新通道未接入（公证落地后启用）」，
   不再误导为「已是最新」。
5. `desktop-reasonix-reference-plan.md`：G 节核对表同步（Composer 增强 ✅、
   CheckUpdate ⚠️→中间态闭环），F 节追加决策 8。
6. wailsjs 经 `wails generate module` 重新生成（含 SavePastedImage 绑定）。

**设计决策**：粘贴图片保存到项目根 `attachments/`（与沙箱边界一致），消息内以 Markdown
相对路径引用；渲染端按需 ReadFile 读取，避免 base64 污染会话上下文。

**影响**：Composer 增强闭环；Reasonix 计划 A.3 全部项落地。
验证：go test（含 6 例新单测）、tsc、vitest 86/86、eslint 0、wails generate module 全过。
