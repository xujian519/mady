# 2026-08-26

## refactor(agentcore): 日卡 M02：agentcore 子包审阅精炼（concurrency/evidence/filecheckpoint/planmode/tasklist/worker）

**背景**：执行 docs/code-review-plan.md 日卡 M02，通读 agentcore 七个子包非测试代码。

**改动清单**：
1. tasklist Store/FileStore 方法重复双行注释去重
2. evidence Ledger 7 个只读查询 Lock→RLock、commandMatches 冗余条件删除
3. filecheckpoint 冗余 continue 删除、isWithinRoot 改 strings.HasPrefix
4. planmode Decide bash 分支合并、stripQuoted inSingle 分支合并
5. worker executor.go 冗余 var _ 编译时验证删除

**验证**：make verify 全绿（lint+check-arch+doc-check+build+test-race，含 tui/desktop 模块）。

**影响**：全部无行为变化。登记不处理：permission/ 子包为敏感路径前缀按规范跳过；worker 包 RegisterDefaultWorkers/EnsureWorker/Catalog.Verify/Registry.RegisterOrUpdate/IsActivated/VerifyAll 非测试零消费但属公共 API 保留。P0 无。

---

## refactor(graph): 日卡 M03：graph/ 包审阅精炼

**背景**：执行 docs/code-review-plan.md 日卡 M03，通读 graph/ 全部 9 个非测试 .go（3.7K 行）；agentcore/manifests 子包仅含 3 个 JSON 数据文件无 Go 代码，其加载逻辑 LoadManifests 已由 M01 覆盖。

**改动清单**：
1. panic 恢复日志 log.Printf → slog.Error 结构化字段（graph.go/pregel.go，对齐 M01 库代码日志规范）
2. degradation.go 手写后缀比较改 strings.HasSuffix
3. JoinOutputs 字符串 += 改 strings.Builder
4. checkpoint.go MemoryCheckpointStore doc 注释与类型错位修正
5. doc.go 使用示例修正：graph.New(graph.WithPregel()) 为虚构 API，改为 NewGraph/NewPregelGraph 真实用法

**验证**：make verify 全绿（lint+check-arch+doc-check+verify-layers+build+test-race，含 tui/desktop 三模块）。

**影响**：全部无行为变化。登记不处理：InterruptableGraph.SetInterrupt 无锁写 map（约定 Run 前调用，涉并发语义保守档记录）；pregel resumeLoop 直通包装器（保留 errPrefix 语义区分价值）。P0 无。

---

## feat(tools): 启用内部 browser 工具并接通专利全文提取（patent_document）

**背景**：浏览器自动化存在两套并行子系统——内部 Go `browser` 工具实现完整但 `cfg.Browser` 全仓库无赋值（生产死代码）；ego-browser 检索管道已可用但 `GetDocument` 全文提取无下游消费方，`MADY_BROWSER_RETRIEVERS=off` 门控仅覆盖新颖性/无效路径（不一致）。

**改动清单**：
- `cmd/mady/tool_ext.go`：统一 Agent 启用内部 `browser` 工具（14 action 多后端，Vision 自动共享）；`buildEgoCompositeRetriever` 接 `MADY_BROWSER_RETRIEVERS=off` 门控。
- `tools/patent_web_search.go`：抽取 `buildPatentRetrievers` bundle 助手（三源+复合），新增 `patent_document` 工具调用 `GetDocument` 提取标题/摘要/权利要求/说明书全文。
- `tools/tools.go`：注册 `patent_document`（readOnly）。
- `retrieval/domain/browser/config.go`：新增 `RetrieversEnabled()` 集中门控助手。
- `bootstrap/init_reasoning.go`：改用 `RetrieversEnabled()`。
- `retrieval/domain/browser/patent_retrievers.go`：标注内嵌 JS 与 learnings/*/tools/*.js 互为镜像、改动须同步。
- `skill/skill.go`：`MadyExtension` 字段标注为有意保留（preview，暂无消费方）。
- `tools/patent_web_search_test.go`：新增 patent_document 注册/调用闭环、空号报错、off 门控 4 个测试。

**验证**：`make verify` 全绿（lint+build+race 全模块 + tui/desktop）；运行级验证启用 Browser+PatentWebSearch 后工具集含 `browser`/`patent_document`/`patent_web_search`，`MADY_BROWSER_RETRIEVERS=off` 时两个检索工具不注册。

**影响**：统一 `mady-agent` 工具集新增 `browser` 与 `patent_document`；PDF 下载仍由 nuo-patent `patent_download` 覆盖（不重复造轮子）。涉及敏感路径 `tools/tools.go`，需留意 CI 标记。

---

## fix(tools): 修复浏览器自动化子系统 code-review 12 项发现（契约/门控/副作用）

**背景**：对浏览器自动化子系统 code-review 输出的 12 项发现（P1×4 + P2×4 + P3×4）全部落地修复。

**改动清单**：
- `tools/browser_tool.go`：`NewBrowserTool` 全局浏览器 manager 改懒初始化（`ensureToolBrowserManager`），移除构造时 `CloseAll`+替换全局副作用，会话跨 agent 重建保留（P1-1）。
- `tools/patent_web_search.go`：`patent_document` 统一响应结构 `patentDocumentResult`（found/title/abstract/content/url/source/truncated/note），全源未命中返回结构化提示；Espacenet biblio（仅目录信息）不再冒充全文，返回 found=true+note；新增 `max_chars` 截断参数（P1-2/P2-6/P3-10）。
- `retrieval/domain/browser/browser_retriever.go`：task space 序号由实例级改为包级 `globalTaskSeq`（多 bundle 并行不再命名冲突）；`GetDocument` 增 `full_text` 元数据标记是否含 claims/description（P1-3/P1-2 前置）。
- `agentcore/worker/catalog.go`：`patent-search-executor`/`patent-search-commander` AllowedTools 补 `patent_document`（P1-4）。
- `cmd/mady/tool_ext.go`：`cfg.Browser` 纳入 `MADY_BROWSER_RETRIEVERS=off` 保密隔离门控，off 时通用 browser 工具一并禁用（P2-5）。
- `retrieval/domain/browser/composite.go`：`GetDocument` 全源失败返回聚合错误（`errors.Join`），区分「全源故障」与「真未命中」（P2-7）。
- `retrieval/domain/browser/patent_retrievers.go` + 装配层：新增 `NewDefaultPatentRetrievers` 共享工厂，tools/cmd/mady/bootstrap 三处复用；bundle 去 `composite` 冗余字段，GetDocument 跳过与 Google 同源的 CNIPA（P2-8/P3-11/P3-12）。
- `retrieval/domain/browser/config.go`：`RetrieversEnabled` 支持 falsy 族（off/false/0/disabled/no，大小写不敏感）（P3-9）。

**验证**：`make verify` 全绿（vet+build+race 全模块 + tui/desktop）；新增/更新测试：composite 全源失败聚合、biblio-only full_text、patent_document 统一形状/未命中提示/max_chars 截断、falsy 门控族。

**影响**：`patent_document` 响应从 `*DomainDocument`/`map` 双形状统一为 `patentDocumentResult`（消费方兼容）；`MADY_BROWSER_RETRIEVERS=off` 语义扩大为同时禁用通用 browser 工具；task space 全局序号跨实例单调递增（行为增强，无兼容破坏）。
