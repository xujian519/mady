# 2026-08-28

## refactor(domains): M08 enablement + inventiveness 代码审阅

**背景**：M08 代码审阅发现 14 处 scanner 吞错告警，均为 LLM JSON 解析降级模式（json.Unmarshal 失败时降级为原始文本）。

**改动清单**：
- enablement/node_clarity.go、node_completeness.go、node_conclusion.go、node_enablement.go、tool.go：补充降级意图注释
- inventiveness/node_step1.go、node_step2.go、node_step3.go、node_step4.go、node_conclusion.go、feedback.go、feedback_tool.go、tool.go：补充降级意图注释

**验证**：`make verify` 全绿；scanner 吞错告警 14 → 0

---

## refactor(domains): M09 novelty + infringement 代码审阅

**背景**：M09 代码审阅发现 7 处 scanner 吞错告警，均为 LLM JSON 解析降级模式（json.Unmarshal 失败时降级为原始文本）。

**改动清单**：
- novelty/prompts.go（5 处）、tool.go：补充降级意图注释
- infringement/nodes_utils.go：补充序列化失败降级意图注释

**验证**：`make verify` 全绿；scanner 吞错告警 7 → 0

---

## refactor(domains): M10 evidence + ipc + checker 代码审阅

**背景**：M10 代码审阅（evidence 20 + ipc 4 + checker 4 共 28 个非测试 .go）发现 4 处 scanner 吞错告警（均为 URL/日期解析失败的保守降级）及若干风格冗余。

**改动清单**：
- evidence/engine.go + public_use.go：evaluatePublicIntent 悬空注释归位
- evidence/date.go、public_use.go、type_specific.go：4 处降级分支补 fail-safe 意图注释
- evidence/date.go、standard.go：extractDateFromText 双 if 合并、AssessProofStandard 中间变量内联
- evidence/date.go + date_test.go：删除仅测试使用的 isBeforeFilingDate（nolint:unused 自证），测试改写为 TestIsBeforeFilingBool 直接覆盖在用函数

**验证**：make verify 全绿（含 tui/desktop 三模块）；evidence/ipc/checker 包级 -race 测试全绿；scanner 吞错告警 4 → 0

**登记待 fix 卡**：checker Catalog.byID 切片指针脆弱模式；RunChecker 未实现占位 verdict 语义；包级/方法 AssessProofStandard 同名不同义

---

## refactor(domains): M11 日卡：claimchart/provisions/provenance 审阅精炼

**背景**：M11 日卡审阅 claimchart/provisions/provenance 三模块（11 个非测试 .go，约 1.7K 行），发现 4 处 scanner 吞错告警、IPC 映射加载与 projectRootUpward 重复逻辑、文件尾悬空节头、变量遮蔽及注释计数漂移。

**改动清单**：
- provisions/domain_agents.go：LoadIpcDomainMap 内联 12 层向上查找改为复用 projectRootUpward；OrDefault 降级分支收敛为零值字面量并补降级契约注释
- claimchart/tool.go、provisions/types.go、register.go、provenance/provenance.go：5 处降级/忽略错误分支补 fail-safe 意图注释（参数解析失败返回工具结果、源文读取失败按无源文降级、溯源 fail-open、OrDefault 契约、注册 fail-open、轮转关闭忽略）
- provisions/provision_agents.go：删除文件尾悬空节头注释
- claimchart/tool.go：chartID 局部变量重命名避免遮蔽同名函数
- provisions/types.go、doc.go：条款簇计数注释 22→23（与 manifest 实际一致）；doc.go 过时入口函数引用 LoadPatentProvisionHandoffs 修正为 RegisterProvisionHandoffs

**验证**：make verify 全绿（含 tui/desktop 三模块）；provisions/claimchart/provenance/domains 包级 -race 测试全绿；scanner 吞错告警 4 → 0

**影响**：无行为变化。登记待 fix 卡——Tier C 领域专家工厂 DomainAgentHandoffConfig 零生产接线（编排器 prompt 与 resolve_domain_workers 引导 LLM 调用的 transfer_to_domain-* 工具从未注册）；register.go 覆盖度告警字段 defined 与 ValidateManifest 口径不一致。RegisterProvisionHandoffs/ListRegisteredProvisions/ProvisionHandoffs/ReasoningHandoffs 仅测试消费，属导出公共 API 按规范保留。

---

## feat(domains): Tier C 领域专家预注册接线（M11 P1 fix 卡）

**背景**：M11 日卡（docs/code-review-plan.md）登记 P1——编排器 prompt 与 resolve_domain_workers 工具引导 LLM 调用 transfer_to_domain-*，但该 Handoff 从未注册（agentcore 仅从已注册 config.Handoffs 生成 handoff 工具），调用必然落空。本卡按已批准计划采用静态预注册方案接线，注册点在 provisions 包内（patent.go 为敏感路径，经 RegisterProvisionHandoffsFromManifest 尾部接线使 PatentAgentConfig 与 BuildProjectAgent 两条装配路径同时生效）。

**改动清单**：
- ipc-domain-map.yaml：10 个已描述 IPC 段标记 pre_register: true，修正插在 ipc_sections 列表中间的注释错位，头注释同步预注册口径
- domain_agents.go：IpcSectionEntry 增加 PreRegister 字段；ListDomainWorkerNames 仅广告预注册段（广告 ⊆ 已注册）；新增 RegisterDomainExpertHandoffs（pre_register 段 × provision_suffixes，映射表不可用降级为 0）；DomainAgentHandoffConfig 抽出 buildDomainHandoff/findIpcSection 供预注册循环复用单次映射表加载；领域专家工具收敛为 DefaultPatentTools 检索集防止递归调用重型分析工具；suffixLabels 后缀→中文领域名
- register.go：RegisterProvisionHandoffsFromManifest 尾部接线，注册摘要增加 domain_count
- 测试：预注册数量/名字格式/无重复断言、发现广告 ⊆ 已注册一致性测试、集成测试补 domain-A61-novelty 存在断言

**验证**：go test -race ./domains/provisions/ ./domains/ 全绿；make verify 全绿（含 tui/desktop 三模块）；scanner 指标全 0；零敏感路径改动

**影响**：行为变化——patent-agent 与 patent-orchestrator 新增 70 个可调用 transfer_to_domain-* Handoff（10 预注册段 × 7 后缀，约 +7K tokens/轮工具 schema）。登记后续卡：工具面 token 实测与 BeforeModelCall 按需过滤、条款智能体直连领域专家（AllowedSources 扩 provision-*）、wiki_card_roots 限域检索承接。

---

## feat(domains): IPC 分类器置信度饱和校准（消除关键词表长度偏差）

**背景**：DSH（deepseek-harness）专利域调研引入项之一。原置信度公式 0.50+ratio*0.5 依赖命中关键词占该部关键词表的比例，关键词表小的大类会被少量命中虚高。

**改动清单**：
1. domains/ipc/ipc_classifier.go：置信度改用饱和函数 confidence = 0.5 + 0.5*(1-e^(-hits/8))，只依赖绝对命中数（hits=8 时约 0.82，恰好越过高置信阈值 0.80），并删除不再使用的 maxWords 字段。
2. ipc_classifier_test.go：新增两条回归测试（同命中数跨大类置信度必须相等、置信度随命中数单调递增）。

**验证**：go test ./domains/ipc/ 通过。

**影响**：置信度数值分布变化（单命中从最高 ~1.0 降至 ~0.56）；IsHighConfidence 语义更严格，需约 8 个不同关键词命中才越过高置信阈值。

---

## feat(session): 会话自动压缩新增 token 阈值触发

**背景**：DSH 调研引入项之一；session/auto_compactor.go 原注释自认仅按消息数触发，少而长的消息会撑爆上下文。

**改动清单**：
1. CompactionConfig 新增 MaxTokens（默认 20000，0 为禁用）。
2. CheckAndCompact 用 agentcore.EstimateMessageTokens（与 agentcore 压缩同一估算口径）估算消息 tokens，消息数或 tokens 任一超阈即触发。
3. 新增守卫：可压缩消息数不超过 KeepRecent 时不动作，防止 token 触发下近几条长消息反复空转压缩。
4. 日志补充 estimated_tokens/token_triggered 字段；新增 3 个测试。

**验证**：go test ./session/ 通过（含既有用例回归）。

**影响**：默认配置下超长会话将更早触发压缩，保护上下文窗口；MaxTokens=0 可显式关闭保持旧行为。

---

## feat(domains): claim chart 逐格 pin-cite 确定性校验

**背景**：DSH 专利域 pin-cite-validator 引入（借鉴 claude-for-legal 的 Every cell pin-cited 思路）：对照表引用幻觉需要确定性校验兜底。

**改动清单**：
1. 新增 domains/claimchart/pincite.go：引文剥空白后必须是源文逐字子串（容忍 PDF 折行/全角空格）；pinCite 段号定位符（[0032] 全半角括号）必须真实存在于源文；源文含段号但本行未定位时给 locator-absent 提示。
2. tool.go：BuildClaimChart 建表后逐行校验，全部通过的行标记 Verified=true，问题清单随 chart.pinCiteIssues 输出并在工具结果中报 pin_cite_issue_count；引文超长改为截取 300 字逐字前缀（不再追加省略号，保证可校验）；命中片段自带段号时 pin-cite 升级为 [D1 [0032]] 形式。

**验证**：go test ./domains/claimchart/ 通过（新增 12 个测试）。

**影响**：ClaimChart 输出新增 pinCiteIssues 字段（向后兼容）；对照表引用可被下游/人工按行核验。

---

## feat(doomloop): 建议性同参重复调用提醒（repeat-tool-reminder）

**背景**：DSH guard/repeat-tool-reminder 引入。doomloop 原只有 Fatal 熔断，缺硬熔断前的温和自纠层。

**改动清单**：
1. 新增 doomloop/reminder.go：同一工具以相同参数连续调用达阈值（默认 3/5/8 次）时向下一轮模型请求注入建议性提醒（advisory，不阻塞不终止）。
2. 参数 JSON 键序归一化判等；真实用户消息（带 [repeat-guard] 标记的注入消息除外）开启新一轮时计数清零。
3. Config 新增 ReminderThresholds/WithReminderThresholds/WithoutReminder，默认开启，经 defaultDoomLoopHook 自动对全部领域 Agent 生效。

**验证**：go test -race ./doomloop/ 通过（新增 9 个测试）。

**影响**：默认开启后，病态重复调用场景每轮请求会多一条提醒消息；WithoutReminder() 可关闭。

---

## feat(domains): 工具结果落盘接线：大结果替换为头尾摘要+落盘句柄

**背景**：DSH spill 机制调研引入项。agentcore.ToolResultBudget（PilotDeck 移植）此前只有库与测试、无运行时接线。

**改动清单**：
1. domains/assemble.go 新增 appendResultOffload：RootDir 固定为 $MADY_HOME/offload/（util.MadyHome 解析失败时跳过接线不阻断启动）。
2. 挂入 appendGatewayLifecycle，与 DoomLoop 同覆盖面（Patent/Legal/Unified/Project 四处装配块）。

**验证**：go build ./domains/ 通过；make verify 全量通过。

**影响**：超过 8KB 的工具结果在持久化前替换为 rune 安全的头尾摘要（各 1500 字）+ 落盘句柄，完整内容按 SHA-256 内容寻址落盘（幂等）；落盘失败保留原结果，绝不把成功调用变成失败。

---

## feat(domains): slop_gate 反套话确定性评分闸门

**背景**：DSH 专利工作台 slop-gate 原子引入：起草产物的套话/空话/无支撑断言需要确定性拦截轨，与 LLM 语义评审并行。

**改动清单**：
1. 新增 domains/slop 包：四类确定性中文套话检查（无数据支撑的效果断言/无引证的结论式评述/空话模板/同套话重复），句级扫描 + 否定词与引证双重误报防线（含否定词或带对比文件引证的句子不命中；效果断言句含数字即视为有数据支撑）。
2. 加权计分后按密度判定 pass/needs_revision 并逐条给修订建议；判定 needs_revision 时返回 revision_hint 供工作流回退重写或人工复核。
3. 新增 slop_gate 工具（ReadOnly，零 LLM 调用），注册到专利域工具面（domains/patent.go）。

**验证**：go test ./domains/slop/ 通过（11 个测试）。

**影响**：模型工具面新增一个只读工具；误报防线为保守方向设计，漏报可由语义评审补位。

---

## feat(novelty): 新颖性图新增确定性数值范围核验节点（step_numeric_range）

**背景**：DSH novelty 子图 numeric_range 节点引入：数值范围是新颖性争辩高频点，需要与 LLM 语义轨并行的确定性判定轨。

**改动清单**：
1. 新增 domains/novelty/numeric_range.go：正则提取权利要求与对比文件中的数值范围/单边表述/数值点（支持 5-10、5至10、至少、不超过、≥ 等写法及单位捕获），按审查指南典型情形做区间重叠判定：破坏性重叠记 overlapped（情形一/二/三），数值点落入范围且无共同端点单独记 inside_without_endpoint（情形五，不破坏新颖性）。
2. 仅带单位的强发现参与判定，无单位的弱发现（如权项编号）不参与；单位不同不可比。
3. 双轨交叉对照：与语义轨 CompareResult.NumericRangeResult 对照，结论不一致时在摘要中显式提示。
4. 图拓扑：step_single_compare → step_numeric_range → step_conflict_check；结论节点将核验摘要并入输入。

**验证**：go test ./domains/novelty/ 通过（8 个测试）。

**影响**：novelty 图每轮分析增加一个纯函数节点（零 LLM 开销）；结论输入新增数值核验摘要段。

---

## feat(workflows): 专利工作流 manifest 支持条件回退声明（retry）

**背景**：DSH 管线即数据的 retry 语义引入（whenOutputMatches + rewindTo + maxRetries）：把"检查不过回退重写"从手写逻辑变成可声明配置。

**改动清单**：
1. domains/workflows/patent manifest 新增 PatentWorkflowStepRetry：步骤可声明产出命中正则时回退到更早步骤重做。
2. 校验 fail-closed：回退正则必须可编译、回退目标必须存在且在当前步骤之前（只允许向后回退）、max_retries 限 1-3。
3. patent_workflow_run 展开时把 retry 字段随可执行计划下发，溯源详情行标注回退声明。
4. patent-drafting.yaml 的 s4-check（checker-review）首批声明回退：命中 '需修订|不合格|缺少|不一致' 时回退 s3-draft 最多 1 次。

**验证**：go test ./domains/workflows/patent/ 通过（10 个测试）。

**影响**：manifest schema 向后兼容（retry 可选）；后续 slop_gate 的 needs_revision 信号可直接作为回退条件消费。

---

## feat(rulekit): 统一判级聚合协议：pass/needs_revision/blocked

**背景**：DSH checker 判级聚合引入。此前各领域规则模块（claimdrafting/specdrafting 等）评分口径不一，工作流质量门禁没有一致的消费接口。

**改动清单**：
1. 新增 domains/rulekit/verdict.go：任一 SeverityError（Must）→ blocked；SeverityWarning（Should）计数达 ShouldBlockedAt（默认 1，DSH 语义）→ blocked；SeverityInfo（Quality）计数达 InfoRevisionAt（默认 3）→ needs_revision；其余 pass。阈值 ≤0 回退默认（防配置错误导致全放行）。
2. 消费方：specdrafting ScoreReport 新增 Verdict 字段（类型别名 rulekit.Verdict），由 scorer 按协议计算。

**验证**：go test ./domains/rulekit/ ./domains/specdrafting/ 通过（6 个新测试）。

**影响**：工作流质量门禁获得统一判级词汇；既有评分/等级字段不变，纯增量。

---

## feat(workercontract): 角色契约与立场配对对抗评审（patent_team_resolve）

**背景**：DSH patent-teams role-contracts 引入：对抗性场景需要"立场配对 + 中立裁判"的契约化角色体系，立场约束靠结构校验而非提示语自觉。

**改动清单**：
1. 新增 domains/workercontract/role.go：Stance 七值立场（neutral/agent-side/examiner/applicant/defense/attacker/judge）；RoleContract 契约 + Validate + PersonaSegment（越界禁止与 HITL 提示显式进 persona）。
2. TeamComposition 立场配对校验：裁判至多一个、裁判必须搭配至少两个不同对抗立场、裁判禁止承担策略起草（结构性保证）。
3. 内置四场景编排：patent-drafting（撰写×对立评审×技术核验×范围审视裁判）、invalidation（请求人×专利权人×合议组）、oa-response（审查员×答复代理人×定稿裁判）、infringement-analysis（权利人方×被控方×技术调查官×裁判）。
4. 新增 patent_team_resolve 工具（ReadOnly），按场景输出 persona 段落供子代理装配，注册到专利域工具面。

**验证**：go test ./domains/workercontract/ 通过（7 个测试）。

**影响**：对抗性分析场景可按场景装配多角色评审；内置编排在 ResolveTeamComposition 时强制过配对校验。

---

## feat(permission): 单调拒绝层（monotonic deny）：凌驾于 allow 配置之上的安全底线

**背景**：DSH EVI-011 单调 deny 模式引入。⚠ 敏感路径改动（agentcore/permission、domains/patent.go），已经仓库维护者人工审阅确认提交。

**改动清单**：
1. Policy 新增 MonotonicDeny（规则型，工具/specifier 粒度）与 MonotonicDenyFns（DenyCheck 谓词型，参数语义粒度）两层硬拒绝；Decide 在一切规则与回退逻辑之前评估，命中即 Deny——任何 allow 配置不得覆盖；单调层为空时行为与既有优先级完全一致（3 个兼容性测试锁定）。
2. domains/evidence/monotonic.go 实现 EVI-011 证据形式要件检查：域外证据未公证认证/外文证据无译本时拒绝 judge_type_specific 调用，拒绝理由含补救指引；形式要件词表硬编码即权威源，修改须人工审阅。
3. judge_type_specific 工具新增可选 notarization_status/translation_status 声明参数。
4. BuildProjectAgent 权限装配接线该检查（patent.go，附敏感路径人工审阅标记注释）。

**验证**：go test ./agentcore/permission/ ./domains/evidence/ 通过（8 个测试）。

**影响**：BuildProjectAgent 装配的专利 Agent 对域外/外文证据判断调用新增形式要件硬门（hint 显式声明 overseas/foreign_language 时触发）；未声明类型不拦截，引擎推断路径不受影响。

---

## feat(invariant): 运行时不变量自检框架与两个 companion（plantask/ipc）

**背景**：DSH runtime-diagnostics/invariants 模式引入：核心包自带生产环境自检，不变量被破坏时报告归属包名。

**改动清单**：
1. 新增顶层 invariant 包：Check{Package,Name,Fn} + 全局注册表（包 init 注册）+ RunAll；MADY_INVARIANTS_DISABLE 正则显式豁免，豁免配置编译错误本身作为违规上报（fail-closed）。
2. Companion 一：agentcore/plantask 校验状态迁移矩阵闭合性（终态不得为源、非终态必有出边、目标均为已知状态）。
3. Companion 二：domains/ipc 校验领域表完整性（八类齐全、关键词非空无重复）——上线即抓到真实数据问题：G 部关键词"检测"重复会虚增命中数扭曲置信度校准，已去重修复（ipc_types.go）。
4. 宿主接线：bootstrap.Setup 统一执行，违规 Warn 记录不阻断启动。

**验证**：go test ./invariant/ ./agentcore/plantask/ ./domains/ipc/ 通过（3 个测试 + 框架用例）。

**影响**：各子命令启动时增加轻量自检开销（纯内存检查）；新 companion 按约定在包 init 注册即可。

---

## fix(domains): brooks-review 评审修复：offload 回读通道、装配层测试锁定、重复实现收敛

**背景**：brooks-review 对 commit 9244fbbc（DSH 引入三批）的 PR 审查产出 3 Warning + 4 Suggestion，用户要求全部修复。

**改动清单**：
1. 【W1】新增 agentcore/tool_result_offload_read.go——offload_read 工具按句柄回读落盘全文（根目录内路径校验防穿越、256KB 防御上限、ReadOnly），摘要提示语同步标注回读通道；appendResultOffload 同步注册回读工具；回环/穿越/未知句柄 3 个测试。
2. 【W3】domains/patent.go 抽取 projectAgentPolicy()（敏感路径接线独立成可测函数）；新增 patent_policy_test.go 装配层锁定测试——单调拒绝接线、offload 钩子与回读工具注册被删除时测试失败。
3. 【W2】AGENTS.md 提交规范补充多特性变更按批次拆分支的流程约定（含 pre-commit 全树编译检查的约束说明）。
4. 【S1】新增 pkg/util/truncate.go TruncateRunes（全仓统一的单端 rune 截断）；slop/claimchart 两处本地实现收敛。
5. 【S2】切句口径收敛到 analysiskit.SplitSentences（新增）；slop 与 claimchart 删除各自的重复正则。
6. 【S3】novelty numeric_range 方向词改为单表驱动（boundDirections map），消除两级 switch 分发。
7. 【S4】appendResultOffload 在 MadyHome 解析失败跳过接线时补 slog.Warn 留痕。

**验证**：make verify（lint + build + test-race，根模块/tui/desktop）全绿；新增测试 8 个（回读 3 + 装配锁定 3 + 截断 1 + 既有回归）。

**影响**：模型工具面新增只读工具 offload_read（注册于全部领域 Agent）；落盘摘要文本新增回读提示（断言摘要格式的测试已同步）；无破坏性变更。

---

## fix(arch): 修复 CI check-arch：注册 invariant 架构组件并授权 agentcore 依赖

**背景**：0d132e82 引入顶层 invariant/ 包（运行时不变量自检框架），但未在 .go-arch-lint.yml 注册组件，导致 CI check-arch job 失败：invariant.go/invariant_test.go 未挂载到任何组件，且 agentcore（plantask/invariant.go:14）导入未注册包被判违规。

**改动清单**：
1. components 注册 invariant 组件（./invariant/**）；
2. agentcore mayDependOn 白名单加入 invariant；
3. 将 invariant 约束为零依赖叶子包（mayDependOn 仅自身，该包只 import 标准库）。

**验证**：本地 go-arch-lint check 通过（OK - No warnings found）；其余 CI job 在原提交已全绿，本次仅改 YAML 不影响 Go 构建。

**影响**：架构门禁恢复绿；invariant 包纳入架构组件治理，后续对其新增依赖须在 archfile 显式授权。
