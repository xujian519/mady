# 2026-09-01

## refactor(workflows): M12 日卡：legal/patent 工作流死代码清理

**背景**：docs/code-review-plan.md M12 日卡（domains/workflows + plantask + workercontract）审阅后的第一批精炼，全部为无行为变化的死代码删除。

**改动清单**：
- legal/reasoning_types.go：删除零消费的本地 SyllogismBuilder struct（比较图实际使用 reasoning.NewSyllogismBuilder）
- legal/comparison.go：删除仅测试消费的包级 caseSearchNode 兼容包装；comparison_test.go 改调 newCaseSearchNode(nil)
- patent/invalidation_nodes.go：删除仅测试消费的 gatherEvidenceNode（生产图从不注册该节点）；invalidation_test.go 改调 newGatherEvidenceNodeWithRetriever(nil)，IsDegraded 断言等价
- patent/reexamination.go：删除口审节点读取后立即丢弃的 draft 死语句
- patent/ground_scan.go：删除 scanGrounds 中永假的 seen[i] 死分支与 seen map

**验证**：`go build ./...` 与三模块 `go test` 全绿；`make verify` 全绿（lint/check-arch/doc-check/verify-layers/build/test-race 含 tui/desktop）。

**影响**：无行为变化；净删 10 行；未触碰敏感路径与工具 inputSchema。

---

## refactor(workflows): M12 日卡：BuildInvalidationGraphFromYAML 检查图构建错误

**背景**：M12 代码审阅发现 BuildInvalidationGraphFromYAML 对 4 处 AddNode 与 4 处 AddEdge 全部使用 `_ =` 丢弃错误，同文件 BuildInvalidationGraphWithOpts 对相同图结构均检查错误，不一致且违反 GO-STANDARDS §0.1#1（所有 error 必须检查）。

**改动清单**：
- patent/invalidation.go：BuildInvalidationGraphFromYAML 的 AddNode/AddEdge 改为检查错误并返回（错误收窄；图结构与 WithOpts 相同、静态可证不失败，正常路径行为不变）

**验证**：`go build ./...` 与三模块 `go test` 全绿；`make verify` 全绿。

**影响**：仅失败路径收窄（图构建异常时返回错误而非产出半成品图），正常路径无行为变化。
