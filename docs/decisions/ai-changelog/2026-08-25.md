# 2026-08-25

## chore(docs): 新增每日代码审阅日卡计划与 Go 气味扫描工具

**背景**：对齐 Sati 日卡机制（docs/code-refinement-plan.md），为 Mady 建立每日代码审阅长期任务——纯人工逐日认领、只做无行为变化清理。

**改动清单**：
1. docs/code-review-plan.md：新增日卡计划（37 卡 6 阶段 + 11 项 Go 审阅清单 + 基线 + 护栏）。
2. scripts/scan_go_smells.py：新增气味扫描脚本（裸 fmt/忽略 err/静默吞错/TODO/未注释导出/长函数六指标，git 跟踪文件 + 目录明细 + --json/--fail）。
3. .claude/skills/mady-daily-code-review/SKILL.md：本地执行技能（不入库）。

**验证**：make verify 三模块全绿；扫描基线 2026-08-25 实测（929 文件/66 裸 fmt/24 忽略 err/380 静默吞错/6 TODO/93 未注释/30 长函数）。

**影响**：新增每日审阅工作流入口，日常由 mady-daily-code-review 技能按进度表取卡执行；纯人工触发，无自动调度。

---

## refactor(agentcore): 收敛重复逻辑与死分支

**背景**：M01 日卡（agentcore 核心）审阅发现 4 处重复逻辑/死分支，属无行为变化清理。

**改动清单**：
1. state.go: messagesReadOnly 委托给 Messages，消除重复深拷贝实现。
2. context_engine.go: compressionProvider 移除未实现字段的死分支（compressionBaseURL/APIKey）。
3. handoff_loader.go: 简化温度默认值的冗余条件（role.Temperature == 0）。
4. system_prompt.go: 提取 sortedSegments 收敛 Build/BuildSegments 段排序。

---

## refactor(agentcore): 库代码日志统一走 slog

**背景**：agentcore 库代码残留 log.Printf 裸日志，与项目结构化日志规范（GO-STANDARDS §0.1#4）不一致。

**改动清单**：
1. event_logger.go: log.Printf → slog.Warn（buffer full/marshal/append 三处）。
2. pubsub.go: log.Printf → slog.Error（subscriber goroutine panic，含 stack）。
3. lifecycle.go: log.Printf → slog.Warn（ObserversToHook 忽略不支持类型）。

---

## refactor(agentcore): 补充 fail-safe 降级与导出注释、修正配置默认注释

**背景**：M01 日卡审阅发现 5 处静默降级/忽略 error 无意图注释、1 处未注释导出、1 处配置默认注释与实现不符，全部为注释治理。

**改动清单**：
1. tool_gen_schema.go/blocks.go/executor.go: 序列化失败降级路径补意图注释。
2. agent_run_phase.go: `_ = failLoop` 次生失败补意图注释（两处）。
3. todopad.go: NewTodoPad 补导出注释（扫描器未注释导出归零）。
4. config.go: KeepRecentTokens 默认注释 2000 → 16384（与引擎实现一致）。
